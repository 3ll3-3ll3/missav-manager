# 2026-07-12 修改日志

本日志记录本轮围绕 Electron 启动、历史番号数据库、Raindrop 官方格式兼容、收藏编辑和 Collection 层级管理完成的修改。

## 1. Electron 启动与构建

- 将 Electron 升级并锁定到当前 `package.json` / `package-lock.json` 配置，当前构建使用 Electron 43.1.0。
- 构建时支持：
  - `ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`
  - `ELECTRON_BUILDER_BINARIES_MIRROR=https://npmmirror.com/mirrors/electron-builder-binaries/`
- 解决最初的 `TypeError: fetch failed` / `Electron failed to install correctly` 启动问题。
- 重新生成 `dist\MissAV_Manager_v0.1.0.exe`。

## 2. Raindrop 官方格式适配

新增 `src/raindrop.js`，实现：

- 解析官方 11 列 CSV。
- 生成官方 11 列 CSV。
- 解析 Netscape Bookmark HTML 中的嵌套 H3/DL Collection。
- 读取 HREF、ADD_DATE、LAST_MODIFIED、TAGS、DATA-COVER、DATA-IMPORTANT。
- 读取 `<DD>` Note 和 `<blockquote>` Highlights。
- 生成带层级 Collection、Note、Highlights、Favorite 的 HTML。

格式结论：

- CSV 是无损主备份，因为它包含 Raindrop ID 和 Excerpt。
- HTML 不含稳定 Raindrop ID 和 Excerpt，但可补充层级、Last Modified、Notes 和 Highlights。
- 同时选择 CSV/HTML 时会合并互补字段。

## 3. 官方 CSV 多行异常修复

修改 `src/csvTools.js`：

- 检测完整官方 11 列表头。
- 修复未加引号的多行 Excerpt。
- 按数字 Raindrop ID 识别下一条记录边界。
- 保留原始多行内容，并把 URL、Folder、Tags、Created 等尾部字段恢复到正确列。
- 普通 CSV 继续使用原有通用解析行为。

修复前示例文件被显示为 4,544 行并出现 1 条错误 URL 提醒；修复后为 4,532 条有效记录、0 个校验问题。

## 4. 收藏主数据库

修改 `src/database.js`，新增通用 `bookmarks` 表：

- 保存 Raindrop ID、Title、Note、Excerpt、URL、Folder、Tags、Created、Cover、Highlights、Favorite、Last Modified。
- 保存可选番号 `code` 和 `source_code_id`。
- 旧 `codes` 记录自动迁移为收藏记录。
- 数据库首次迁移前自动备份。
- 允许多条收藏关联同一个番号索引。
- 允许相同 URL 以不同 Title/Collection 作为独立收藏存在。

新增 API：

- `getBookmarkLibrary`
- `getBookmarkStats`
- `importRaindropRecords`
- `exportRaindropRecords`
- `createBookmarkRecord`
- `updateBookmarkRecord`
- `deleteBookmarkRecord`

关键删除规则：删除收藏不删除 `codes`，确保历史去重能力不会因用户整理收藏而丢失。

## 5. Collection 独立目录模型

新增 `bookmark_collections` 表和对应迁移：

- 从现有收藏 Folder 自动建立全部父目录。
- 支持没有收藏的空目录。
- 统一 `父级 / 子级` 路径格式。
- 新建目录时自动补齐父目录。
- 重命名目录时同步迁移全部后代目录和收藏。
- 删除目录时删除目录子树内收藏，但保留永久番号索引。

新增 API：

- `getBookmarkCollections`
- `getBookmarkCollectionInfo`
- `createBookmarkCollection`
- `renameBookmarkCollection`
- `deleteBookmarkCollection`

## 6. 收藏库 UI 重构

修改 `renderer/app.js`、`renderer/index.html` 和 `renderer/styles.css`：

- 收藏库改为 Collection 树、记录列表、详情编辑器三栏布局。
- 左侧 Collection 从平铺字符串改为父子树。
- 支持展开/折叠、创建顶级文件夹、创建子文件夹、重命名和删除。
- 父文件夹数量显示其整个子树收藏总数。
- 点击父文件夹筛选父级及全部子级记录。
- “新增番号”改为更准确的“新建收藏”。
- “选择本页”改为“选择当前范围”，可跨分页选择当前搜索/筛选/目录子树的全部记录。
- 删除目录前显示子目录数和收藏数，并自动创建备份。
- Favorite 可在列表直接切换。
- 记录可单条删除或批量删除，番号历史继续保留。

详情编辑器现支持：

- Title
- Link
- Collection
- Tags
- Note
- Created
- Excerpt
- Highlights
- Cover
- Favorite
- Raindrop ID
- Last Modified
- 番号索引和旧女优/类型 Tag

## 7. CSV 工作台

- 自动识别官方 Raindrop CSV。
- 主表仅显示高频字段，减少视觉噪声。
- 右侧详情编辑全部 11 个字段。
- 修复详情输入时每字符重绘造成的焦点丢失。
- 表格与详情双向同步。
- Favorite 筛选只读取官方 `favorite` 列。
- 保存时保持隐藏字段和官方列顺序。
- 可直接将当前官方 CSV 导入本地收藏库。

## 8. 导入、导出和去重

- 顶部导入入口可一次选择多个官方 CSV/HTML 文件。
- 导入前自动创建数据库备份。
- 带 ID 记录按 ID 合并。
- 无 ID HTML 优先按 URL + Title + Collection 精确匹配。
- 避免把相同 URL、不同 Collection 的合法重复收藏合并。
- 新收藏中识别出的番号自动写入永久 `codes` 索引。
- 本地收藏库可直接导出官方 CSV 或 HTML。
- 导入比较继续使用整个历史番号索引，不受收藏删除影响。

## 9. 文档

更新：

- `README.md`
- `使用教程.md`

新增：

- `AGENTS.md`
- `docs/PROJECT_HANDOFF.md`
- `docs/CHANGELOG_2026-07-12.md`

## 10. 自动测试和真实夹具验证

新增 `test/`，当前共有 14 项自动测试，已全部通过。

覆盖：

- CSV 引号、重复表头和异常多行修复。
- Parser 标准番号和 FC2。
- Fetcher 状态分类、重定向和网络错误。
- Raindrop CSV/HTML 往返。
- 数据库备份恢复。
- 官方 ID 合并。
- 相同 URL 不同 Collection 保留。
- 收藏与番号索引解除。
- Collection 空目录、新建、父目录重命名和删除。

最终最低验证结果：

```text
npm run check   PASS
npm test        14/14 PASS
git diff --check PASS（仅 CRLF 提醒）
npm run build:portable PASS
```

## 11. 尚未完成或建议继续

- 对最新版 Collection 树补做真实 4,582 条收藏的 Windows 截图验收；上次应用控制授权超时。
- 加入拖放移动收藏和目录。
- 加入 Shift 连选、Ctrl 多选和列表全选复选框。
- 用目录选择器替代批量移动时的纯文本输入。
- 设计空 Collection 的 HTML 导出方案。
- 增加最近一次批量操作的撤销入口。
- 在发布新版本前审查 `package-lock.json` 大范围差异，并决定是否提升版本号。

## 12. Collection 交互修复（21:51 构建）

- 定位“新建文件夹无效”的根因：Electron 不支持浏览器 `window.prompt()`。
- Collection 新建和重命名改用应用内模态输入框，支持 Enter 提交、Escape/点击遮罩取消。
- 普通文件夹支持右键菜单和常显 `⋮` 菜单：打开、选择当前范围、新建子文件夹、重命名、删除。
- “全部收藏”右键菜单新增“清空全部收藏和文件夹”。
- “未分类”右键菜单新增“清空未分类收藏”。
- “全部收藏”和“未分类”明确作为系统视图保留，不伪装成普通可删除文件夹。
- 展开/折叠控件从不明显的 `›/⌄` 改为高对比度 `▸/▾`。
- 右键菜单移到窗口根层，修复菜单相对鼠标位置偏移。
- 新增范围删除数据库 API；清空全部时删除收藏和实际 Collection，但保留 `codes` 永久番号索引。
- 修复初始化/恢复流程：已有 `bookmarks` 表时不再重复执行旧番号迁移，防止用户清空收藏后重启又被 `codes` 自动重建。
- 自动测试覆盖“清空未分类”“清空全部”“重启不重建收藏”，14/14 通过。
- Windows 可视化验收确认新建弹窗和右键菜单正常；未执行任何破坏性确认。

数据安全记录：当前真实库为 0 收藏，但自动备份 `missav_data_20260712_212520_delete_collection.db` 仍包含完整 4582 条收藏。未擅自恢复。

## 13. 本地版本信息口径更正（2026-07-21）

- 明确以本地 `dist` 目录中实际生成的最新便携版 EXE 作为当前版本依据，不使用 GitHub Release、Git 标签或提交记录判定最新版。
- 当前本地最新版为 `dist\MissAV_Manager_v0.1.0.exe`。
- 当前改造完成后已重新构建本地 EXE；文件最后写入时间为 `2026-07-21 17:26:07`，大小为 `412,706,073` 字节。
- 核验 EXE 文件版本和产品版本均为 `0.1.0`。
- 当前 SHA-256 为 `8815ADAF6074A9E28D757BC2B35EDFF8DDBA2AB0173A0200BA1B430A04316420`。
- 同步修正 README、使用教程和项目交接说明中的版本来源及旧构建信息。

## 14. 完整数据库管理与导出修复（2026-07-21）

- 新增顶部“完整数据库”页签，默认显示 `bookmarks` 收藏主表，并补齐 `bookmark_collections` 等缺失表。
- 原始表改为分页读取，显示当前页、筛选后总数和全表总数，解决固定上限导致数据看似不全的问题。
- `bookmarks` 编辑和 Collection 重命名改走安全数据库接口，避免直接改表造成关联不同步。
- 收藏地图、逐条整理、导出预览和数据库导出统一改读 `bookmarks` 主表，普通网页收藏不再因为没有番号而漏掉。
- 导出预览与真正导出共用 `buildRaindropExport()`：官方 11 字段、默认 Title/Collection/Tags/Created、有效链接过滤完全一致。
- 空链接和非 HTTP/HTTPS 链接会明确列为排除；内部 SQLite ID 不再误写为 Raindrop ID。
- 修复批量操作混用收藏 ID 与番号 ID、清空番号后返回旧关联、导入时 Favorite 缺省覆盖、相同 URL 不同 Collection 错误合并等问题。
- 收紧普通网页标题的番号推断，避免把 `Example 123` 一类标题误建为永久番号。
- 自动测试增至 15 项，`npm run check` 与 `npm test` 均通过。
- 使用隔离临时数据库完成 Windows 界面验收：208 条收藏完整显示并分页，206 条有效记录可导出，2 条坏链接按预览结果排除；用户真实数据库未被读写。

## 15. 产品收敛为永久番号库（2026-07-21）

- 按用户最新决定，当前产品退出本地收藏、Favorite、Collection、收藏地图、导出预览和 CSV 工作台的正常操作入口。
- 保留旧 `bookmarks` / `bookmark_collections` 表和兼容 API，不自动删表、不清空历史数据，也不再从 `codes` 自动生成收藏。
- `codes` 成为当前番号管理、历史去重和底层数据默认主表。
- 番号管理页改为记录列表 + 右侧详情两栏布局，整条记录均可点击选择。
- 新增 Windows 文件管理器式选择：单击替换、Ctrl 切换、Shift 连选、Ctrl+Shift 追加区间。
- 选择算法抽为 `renderer/explorer-selection.js`，加入单选、切换、区间替换、区间追加和取消选择测试。
- 批量工具收敛为状态、女优 Tag、类型 Tag、生成 URL、番号规范化和永久番号删除。
- 右侧详情收敛为番号、MissAV URL、状态、女优 Tag、类型 Tag 与创建时间。
- 新增番号、女优/类型 Tag、新建备份、批量 Tag 和底层新增记录改用应用内输入框，避开 Electron 中不可用的浏览器 `prompt()`。
- 主导航和库内页签重组为番号、记录、标签、历史，以及番号管理、导入去重、标签管理、维护。
- 维护区收敛为概览、体检、备份、重复项、底层数据和处理历史；底层数据隐藏旧收藏兼容表。
- Raindrop HTML / CSV 继续从“本次处理结果”导出，作为下游导入文件。
- README、使用教程、AGENTS 和项目交接说明同步到当前产品方向。
- 自动测试增至 16 项，覆盖“创建番号不生成 bookmark”以及完整的文件式选择语义。

## 16. 收紧“页面待核验”并修复跳转（2026-07-21）

- 修复候选 MissAV 地址返回 301/302 后没有真正跟随跳转的问题，最多跟随 3 次并保留最终 URL。
- 页面 URL 本身不再作为番号命中证据；首页、保护页和通用错误页不会仅因地址里含番号而进入待核验。
- 新增 Cloudflare Challenge、验证码、403、429、限流和常见访问拒绝页识别，统一标记为 `network_error`。
- `404/410` 以及 HTTP 成功但正文不包含目标番号的页面归为 `not_found`。
- 只有 HTTP 成功、正文确实包含目标番号，但内容过短或缺少详情页证据时，才标记 `need_manual_check`。
- 一个候选地址证据不足时继续检查其他候选地址；后续找到完整详情页会优先升级为 `ok`。
- 所有候选均未确认且出现网络/访问限制时归为 `network_error`，不再错误提升成“可点待核验”。
- `network_error` 写入永久历史以便下次重试，但不会进入本批 Raindrop HTML/CSV。
- 历史 `network_error`、`not_found`、`need_manual_check` 和 `page_ok_play_unknown` 都允许重新查询。
- 结果页将原“可点待核验”改为“页面待核验”，并显示网络失败的具体原因。
- 自动测试增至 20 项，覆盖通用页面、防爬/限流、候选状态归并、网络错误导出排除和跳转超时传递。

## 17. 网络、导入、日志与过滤结果修复（2026-07-21）

- 修复 Electron `net.fetch` 不支持 `redirect: 'manual'` 导致所有候选地址显示 `Electron net: Redirect...` 的问题，改为受支持的 `redirect: 'follow'`。
- 使用 Electron 真实网络冒烟请求验证 `https://missav.ai/cn/jul-991` 返回 HTTP 200、正文 316176 字节且包含目标番号。
- 修复删去旧 CSV/收藏按钮后 `updateUI()` 仍设置 `btnSelectTagFile.textContent` 的空引用；文件导入不再在成功读取后误报失败。
- 原始文字下方新增独立“过滤后的番号”只读文本框，实时按一行一个显示最终结果。
- 新增持久化运行日志，记录数据库启动、文件导入、处理批次、每个候选 URL、HTTP 状态、传输方式、状态归并、导出和未捕获错误。
- 运行日志位于用户数据目录 `logs\missav-manager.log`，5 MB 自动轮换，不记录原始 TG 全文或页面 HTML。
- “番号库 -> 维护 -> 运行日志”支持刷新、复制诊断报告、导出日志和打开日志目录。
- 新增日志写入、尾部读取和轮换自动测试；自动测试增至 21 项。

## 18. 修复正常详情页被误判为访问验证（2026-07-21）

- 根据真实批次日志定位：正常页面虽然返回 HTTP 200、包含目标番号和详情标签，但公共脚本中的 `captcha` / `challenge-platform` 字样被旧规则误判成验证页。
- 访问验证识别改为强弱两级：明确的 Cloudflare 拦截标题和挑战结构仍直接判为 `network_error`；通用脚本词只有在页面缺少“目标番号 + 详情结构”时才算拦截证据。
- 日志中的访问验证错误现在会附带命中特征，便于区分真实限流与规则误判。
- 已确认详情页后立即停止检查其余后缀和镜像候选，常见成功番号从最多 5 次请求降为 1 次，降低大批量处理耗时和触发限流的概率。
- `fetchPage` 保留实际网络传输方式，候选日志不再丢失 `electron-net` / `node` 信息。
- Electron 真实联网复核：`HMN-858` 返回 200，判定 `ok`，抓到 1 个女优标签和 10 个类型标签；`MARK-1232` 返回 404，仍判定 `not_found`。
- 自动测试增至 24 项，覆盖正常详情页携带 CAPTCHA/Challenge 脚本、独立验证码页和成功候选提前结束。
- 新便携版已生成：最后写入 `2026-07-21 18:34:54`，大小 `412,708,294` 字节，SHA-256 `697C1649667D2A43EB2F266B8674D82D84E842A30DAA388B9AF504ECEA72F7A0`。

## 19. 处理界面增加预计剩余时间（2026-07-21）

- “运行控制”和“处理结果”页新增动态预计剩余时间板块，自动切换到结果页后仍可查看。
- 同时显示预计剩余、预计完成时间、已用时间、单条平均耗时和完成数量。
- 完成首条记录后开始估算，使用最近最多 20 条实际耗时；当前请求明显变慢时会把已等待时间计入估算。
- 准备、运行、正在停止、已停止和已完成状态均有独立显示；清空批次时重置预计时间。
- 处理进度改为完成一条后再增加，避免原来“刚开始处理就提前加一”的视觉误差。
- 新增预计时间纯函数和自动测试；自动测试增至 26 项。
- 使用隔离数据库完成 1440×900 Electron 截图验收，用户正式数据库未被读写。
- 新便携版已生成：最后写入 `2026-07-21 19:44:01`，大小 `412,720,688` 字节，SHA-256 `FDC0EC94D6977CE5E6A798C008C058919EE093FB7BA9B5B52A47166419955906`。

## 20. 增加可调处理速度模式（2026-07-21）

- 运行控制新增“稳妥 / 智能 / 高速 / 极速简查”四档按钮，选择保存到本地并在批次运行期间锁定。
- 稳妥模式保持单条顺序、完整候选和 900ms 行间隔。
- 智能模式从 2 路并发开始，连续健康响应后升到 3；遇到网络错误自动退避到 1 路、增加请求间隔，并对当前候选重试一次。
- 高速模式使用 4 路并发但仍完整检查候选；不额外重试，可能增加临时访问限制。
- 极速简查使用 6 路并发且只查主地址，界面和结果备注明确提示可能漏掉字幕版、泄漏版及镜像页。
- 并发结果按原始输入顺序展示，数据库仍在每条完成后立即写入；结果表最多约每 180ms 批量刷新，降低大批量重绘开销。
- 停止批次时不把未完成候选查询的当前番号误写为 `not_found`。
- ETA 已按当前并发数修正，并显示所选模式、当前并发和估算样本数。
- 自动测试增至 28 项；隔离数据库真实联网验收 4 条记录，智能模式约 6 秒完成，状态和顺序均正确。
- 新便携版已生成：最后写入 `2026-07-21 20:02:53`，大小 `412,733,833` 字节，SHA-256 `F6B18304DA94AB22F5EFFEA6F46A9321F8F404CC1D4AFDF902512D04D1604BBF`。

## 21. 扩展 8 / 12 / 16 路速度档位（2026-07-21）

- 根据用户真实极速日志增加“飞速 8”“狂飙 12”“极限 16”三个主地址简查档位，当前共七档。
- 用户日志截取到的最后 795 条在约 95.8 秒完成：727 条正常、67 条 404、1 条网络错误、0 次重试，约 8.3 条/秒，证明 6 路环境仍有余量。
- 6、8、12、16 路档位均增加网络错误自动保护：临时加大请求间隔并降低新任务并发，稳定后恢复所选档位。
- 16 路隔离真实联网测试：32 条输入中 9 条历史跳过，23 条联网约 2.09 秒完成，21 条正常、2 条 404、0 条网络错误，约 11 条/秒。
- 16 路相比 6 路已开始收益递减，因此暂不增加 24/32 路档位。
- 七档按钮在 1440×900 下以两行布局显示，截图验收无溢出或遮挡。
- 因旧版 EXE 仍在运行，为避免覆盖用户当前批次窗口，新便携版独立保存为 `dist\MissAV_Manager_v0.1.0_speed7.exe`；最后写入 `2026-07-21 20:23:46`，大小 `412,735,398` 字节，SHA-256 `275C55BEE29EA08B713D3E3F9900D2F4FDD42A63DC67D1261107E4B39CB79DCA`。

## 22. 网络错误重跑、按标签导出与表格工作台（2026-07-21）

- 结果表中的 `network_error` 新增行内“重新运行”，并提供重跑选中网络错误、重跑全部网络错误；重跑使用当前速度档位并在原位置替换结果。
- 结果表支持搜索、状态筛选、最终 Tag 筛选、番号/状态/Tag 排序、整行单选、Ctrl 切换、Shift 连选、Ctrl+Shift 追加区间和复制 TSV。
- 导出新增“导出所选/筛选”与“按标签拆分”；后者为每个最终 Tag 生成独立 HTML / CSV，同时生成标签索引和当前范围报告。
- 所有主要内部表格统一增加拖动列宽、双击自动适配、单元格定位、方向键导航和复制能力；各表原有编辑与批量操作不受影响。
- 修复智能模式小批次并发边界：原来 3–8 条任务可能因第 3 个线程等待升档条件而不结束；现在使用活动工作槽调度，先确认队列仍有任务再等待并发许可。
- 自动测试增至 31 项并全部通过；隔离 Electron UI 验收完成 3 条网络错误重跑、标签筛选、番号排序、Ctrl 多选与 4 组/10 文件标签导出，未使用正式数据库。
- 验收截图：`artifacts/ui-results-workbench.png`。
- 本地标准便携版已重新生成：`dist\MissAV_Manager_v0.1.0.exe`，最后写入 `2026-07-21 20:51:11`，大小 `412,770,301` 字节，文件版本/产品版本 `0.1.0`，SHA-256 `67F0EE2284C26EC23423A7E35F00CDD502CD394894D0BFB7C847B04B725AC06A`。

## 23. 七档速度主线升版为 v0.1.1（2026-07-21）

- 按用户决定，以 `dist\MissAV_Manager_v0.1.0_speed7.exe` 作为七档速度主线基准；保留该基准文件，不覆盖、不删除。
- 当前重跑、按标签导出和表格工作台作为该主线的正式续版，版本号由 `0.1.0` 升为 `0.1.1`。
- `package.json`、`package-lock.json`、界面版本徽标、初始化日志、诊断报告、README、教程和交接文档已统一为 `0.1.1`。
- 界面和诊断日志改为读取 `package.json` 的版本号，避免以后出现已升版但界面仍显示旧版本的问题。
- 构建脚本改为从 `package.json` 自动生成目标文件名，并只检查目标版本进程是否正在运行；旧版窗口可保留用于对照，不再误阻止新版本构建。
- 隔离 Electron UI 验收确认版本徽标显示 `v0.1.1`，网络错误重跑、筛选、排序、选择和按标签导出继续通过；31 项自动测试全部通过。
- 新便携版：`dist\MissAV_Manager_v0.1.1.exe`；最后写入 `2026-07-21 21:14:19`；大小 `412,770,562` 字节；文件版本/产品版本 `0.1.1`；SHA-256 `67F4FE3695FEE7F8EC4CE652385C37ACA0C87E1C121B26A94E85BD876A7CFCAA`。

## 24. 根据真实 59 条待核验反馈增强番号过滤（2026-07-21）

- 使用用户提供的 59 条待核验报告和 4,532 行原始 Raindrop CSV，逐条反查番号出现字段、原始标题、文件夹和来源链接。
- 59 条中保留 13 条有明确影片证据的番号：`SIVR-336`、`OFJE-473`、`OFJE-357`、`NSPS-234`、`OFES-022`、`ABF-356`、`JUVR-294`、`KIWVR-849`、`NAMHVR-002`、`OTIM-648`、`SAVR-1069`、`VRKM-1654`、`MIUM-1047`。
- 剔除 46 条由封面文件名、普通网页 URL、论坛帖子号、课程年份、软件版本、站点名称、写真编号和同人商品号造成的误报。
- Raindrop CSV 改为按列解析：只从 `title` 和可信 AV 站点 `url` 提取；明确忽略 `id`、`created`、`cover`、普通 URL、`excerpt` 等字段。
- 普通文本、Telegram HTML 和 Markdown 仍保留通用提取，但通用匹配会先遮蔽 HTML 标签、普通 URL 和图片路径；标准非 FC2 番号限制为 2–8 位字母前缀加 2–5 位数字。
- 增加 123AV、MissAV、AVBase、JavDB、JavBus、JavLibrary 等可信 AV 域名识别，并处理 `393otim-648-uncensored-leaked` 一类带站点数字前缀的 URL。
- 完整源文件回放从旧规则的 3,198 个候选降到 2,830 个，减少 368 个噪声；目标 59 条结果稳定为保留 13、剔除 46。
- 额外剔除 AV 页面摘要中的 `CAMPAIGN-30`、`PRESTIGE-20`、`SUPPORT-30` 等促销词误报；摘要不再参与结构化 CSV 番号提取。
- 自动测试增至 36 项，包含 59 条反馈分类回放；隔离 Electron UI 验收确认 `v0.1.2` 及既有重跑、筛选、排序、选择、按标签导出无回归。
- 新便携版：`dist\MissAV_Manager_v0.1.2.exe`；最后写入 `2026-07-21 21:29:40`；大小 `412,775,874` 字节；文件版本/产品版本 `0.1.2`；SHA-256 `34E4DA80DFE4992A00B8582FB2D39570C3BF9B8DB6B698E41470D4FB53BE9480`。

## 25. 永久处理批次与断点续跑（2026-07-21）

- 版本升为 `0.1.3`，继续以 `v0.1.0_speed7` 为基准主线，并保留 `v0.1.2.exe` 作为上一版对照。
- 扩展 `processing_runs`：保存批次名称、来源类型/说明、速度模式、运行状态、输出目录、完成数、网络错误数和待核验数。
- 新增 `processing_run_items`：逐条保存番号、原始位置、队列状态、结果状态、链接、女优/类型/最终 Tags、导出标记、错误、备注、尝试次数和时间。
- 开始访问网络前先创建完整批次；每条开始时标记 `running`，完成后立即保存结果并关联永久 `codes` 主表。
- 手动停止会把批次保存为 `paused`；异常退出后重新启动时，遗留的 `running` 批次转为 `paused`，运行中的明细回到 `queued`，已完成明细不变。
- 运行控制新增批次名称和最近未完成批次入口；番号库新增独立“处理批次”页签，可搜索、查看来源/进度/逐条错误、重命名、载入结果和继续剩余项。
- 网络错误重跑也会建立独立重试批次，避免重跑过程只存在于内存。
- 批次仅保存过滤后的番号和处理结果，不保存原始 TG 全文，不创建 Favorite 或 Collection，也不复制永久番号主数据。
- 新增批次持久化、跨启动恢复和永久历史不丢失测试；自动测试增至 37 项并全部通过。
- 隔离 Electron 窗口验收因系统审批服务返回 HTTP 503 未能启动；未绕过权限，数据库与静态检查正常完成。
- 便携版首次构建在下载打包组件时遇到网络 `EACCES`，联网权限审批服务随后返回 HTTP 503；确认未生成 `v0.1.3.exe`，因此本地最新版口径暂时仍为 `v0.1.2.exe`。
- 用户明确允许后联网构建成功；隔离 Electron 验收确认批次页仅激活番号库面板，暂停批次显示 1/2 完成、1 条待处理、来源和明细完整，并保留网络重跑、筛选、排序、多选及按标签导出能力。
- 新便携版：`dist\MissAV_Manager_v0.1.3.exe`；最后写入 `2026-07-21 22:26:18`；大小 `412,817,777` 字节；文件版本/产品版本 `0.1.3`；SHA-256 `2C7282D0E8141C82CCF20A24C472CBBC8EC36213C2863EC36A64FB6781CEF120`。

## 26. 四阶段独立任务状态基础（2026-07-21）

- 版本升为 `0.1.4`，新批次使用 `pipeline_version=2`；旧批次保持 `pipeline_version=1` 的 MissAV 单支线语义，不自动补建 123AV 待办。
- 新增 `processing_item_tasks`，每条番号固定保存 `missav/lookup`、`raindrop/sync`、`123av/lookup`、`123av/favorite` 四项任务。
- 历史正常番号只跳过 MissAV 和本次 Raindrop 导出，123AV 查询仍为排队；本批重复项四路均跳过。
- MissAV 成功只把 Raindrop 置为“待导出”，未找到则跳过 Raindrop，网络错误和人工核验保持阻塞；两项 123AV 状态不受 MissAV 结果影响。
- 任务汇总独立于旧 MissAV 批次统计，批次列表和详情分别显示四阶段完成、未闭环和异常数，不再把 MissAV 完成误写成全流程完成。
- 查询任务中断后可安全回到队列；有副作用的 Raindrop 同步和 123AV 收藏若在运行中中断，会进入 `verify_required`，避免远端已成功时盲目重复执行。
- 创建批次、写入单条 MissAV 结果、更新任务依赖、暂停和删除批次均使用事务；删除批次同步清理明细与任务，并在数据体检中检查孤立任务。
- 结果表和批次明细增加四状态列、环节筛选、状态筛选、带提示的外链/重试图标和四阶段 TSV；旧批次明确显示“旧版未启用”。
- 底层数据页新增只读友好的“批次逐条结果”和“四阶段任务状态”表；桌面和 416px 窄屏隔离 UI 验收通过，表格在自身区域横向滚动且不会挤成零高度。
- 当前阶段不实际登录 123AV、不点击账号收藏，也不把 Raindrop `ready` 伪装成已同步；执行器属于下一阶段。
- 自动测试增至 45 项，覆盖旧架构幂等升级、新任务初始化、任务独立性、异常恢复、事务回滚与孤立任务清理。
- 新便携版：`dist\MissAV_Manager_v0.1.4.exe`；最后写入 `2026-07-21 23:21:16`；大小 `412,849,171` 字节；文件版本/产品版本 `0.1.4`；SHA-256 `564651B0E3AD8C82F5088BF732D5F1BA409FEB8285CD8A75210B82671F34F345`。

## 27. 123AV 只读查询执行器（2026-07-22）

- 版本升为 `0.1.5`，新增独立 `src/av123.js`；使用 123AV 公开搜索页查询，不读取账号会话、不登录、不点击收藏。
- 根据真实页面核验确定：搜索会返回大量模糊结果，只有结果卡标题或详情字段中出现精确番号才算命中；标准详情链接优先于无码泄露等变体。
- 识别公开搜索页、详情页、零结果、模糊结果、登录要求、Cloudflare/验证码、403/429、限流、服务端错误和未知页面，并分别写入 `succeeded`、`not_found`、`network_error` 或 `manual`。
- 主进程增加仅允许 `123av.com` / `www.123av.com` 的独立网络 IPC；MissAV 原有域名白名单没有放宽。
- 新批次完成所需 MissAV 查询后，会对全部非重复番号执行 123AV 查询；历史 MissAV 命中不能跳过 123AV，MissAV 未找到或网络错误也不能阻止 123AV。
- 123AV 结果只写 `processing_item_tasks`，不覆盖 `processing_run_items.url/result_status`、`codes.best_url/status` 或 MissAV 标签。
- 123AV 命中后只把收藏任务解锁为 `ready`；未找到时收藏变为 `skipped`，网络/人工异常保持 `blocked`，没有账号收藏动作。
- 停止和异常退出会把运行中的只读查询退回 `queued`；继续批次可只补 123AV，不重开已完成的 MissAV/Raindrop 状态。
- 结果页行内重试与批量重试跟随当前环节筛选；123AV 网络错误直接写回原批次，并保留另一支线状态。
- 运行日志只记录番号、URL、HTTP 状态、分类和候选数量，不记录页面 HTML、Cookie、Session 或账号数据。
- 自动测试增至 55 项；隔离 Electron 验收确认 123AV 成功、待收藏依赖、批次恢复、桌面与 416px 窄屏布局通过；真实公开搜索页返回 HTTP 200、65,639 字节并包含精确结果卡。
- ASAR 内容与打包后隔离启动通过；新便携版为 `dist\MissAV_Manager_v0.1.5.exe`，最后写入 `2026-07-22 11:36:42`，大小 `412,874,788` 字节，文件版本/产品版本 `0.1.5`，SHA-256 `4F39E9C5A006B0A3E44BFFDA7212C11EBAAD44185C9B85A49A663C51CDCCCF27`。

## 28. MissAV 与 123AV 操作分板块（2026-07-22）

- 版本升为 `0.1.6`；数据库四任务结构和“两个站都查询”的业务规则不变，只重组界面职责。
- 运行控制新增 MissAV、123AV 两张独立状态卡，分别显示查询完成数、异常数和当前活动支线。
- 结果页新增 MissAV / 123AV 工作区切换：MissAV 只显示页面查询、Raindrop、女优/类型/最终 Tags；123AV 只显示精确查询和收藏状态。
- 两个工作区分别拥有本站环节选项、分类页签、搜索字段、状态筛选、列宽记忆和 TSV 列定义，避免查询链接、错误和状态跨站点混排。
- 网络错误重跑只作用于当前查询工作区；切换到 Raindrop 或收藏状态时不显示无关的批量重跑按钮。
- Raindrop 范围导出和按标签导出只显示在 MissAV 工作区，123AV 工作区不会出现下游 Raindrop 操作。
- 处理批次详情将四项任务组成两张独立支线汇总卡，并用 MissAV 明细 / 123AV 明细切换表格；单表不再并列四个任务列。
- UI 冒烟测试增加双工作区、分支汇总、桌面和 416px 窄屏断言；自动测试仍为 55 项。
- ASAR 内容和打包后独立临时数据库启动通过；新便携版为 `dist\MissAV_Manager_v0.1.6.exe`，最后写入 `2026-07-22 12:07:45`，大小 `412,895,603` 字节，文件版本/产品版本 `0.1.6`，SHA-256 `028D1ED3C6EB78CCD548830163CD25D3BB9276497499AF23713F933807E23E67`。

## 29. 123AV 可恢复账号收藏（2026-07-22）

- 版本升为 `0.1.7`；123AV 公开查询与账号收藏继续保持独立职责，账号动作只写 `processing_item_tasks` 的 `123av/favorite`，不覆盖 MissAV、Raindrop、永久番号链接或 Tags。
- 新增应用内独立持久化 123AV 账号窗口。用户手动登录一次后可复用登录态；该窗口与 Chrome/Codex 浏览器互不共享会话。
- 程序不读取密码、Cookie、Local Storage、Session Storage 或页面 HTML，只读取可见账号标识、详情番号和“保存/已保存”按钮状态。
- 根据真实页面只读勘察确认：当前账号菜单可见，未收藏详情页按钮为“保存”，已收藏详情页和“我的收藏”列表按钮为“已保存”；站内“导入列表”只支持按其他账号用户名迁移，不能批量粘贴番号。
- 收藏采用单线程幂等状态机：先核对精确番号和远端状态，原已收藏直接成功，只有未收藏才点击；点击后必须再次看到“已保存”才记为成功。
- 点击后网络中断、页面异常、程序退出或确认超时统一进入 `verify_required`；复查操作只读取远端，已收藏改为成功，未收藏退回 `ready`，不会盲目再次点击。
- 123AV 结果板块新增账号状态、打开账号窗口、检查登录、行内收藏/复查、收藏所选、收藏全部待收藏、复查所选、复查全部和停止收藏；执行前确认框明确显示账号与数量。
- 新增 `not_logged_in` 任务状态；登录失效或验证码会停止当前批量并要求用户在账号窗口处理，未开始的记录保持原状态。
- 新增 7 项账号状态机测试和 1 项数据库隔离测试，自动测试由 55 项增至 63 项；真实 Electron DOM 冒烟确认首次只点击一次、二次识别原已收藏且零点击。
- 隔离桌面与 416px 窄屏 UI、ASAR 内容、打包后临时用户目录启动均通过；正式数据库和真实 123AV 收藏均未用于测试。
- 新便携版：`dist\MissAV_Manager_v0.1.7.exe`；最后写入 `2026-07-22 12:43:37`；大小 `412,930,325` 字节；文件版本/产品版本 `0.1.7`；SHA-256 `C4794D34E3912331FEC1814CDF61F435BBCC2F743E5EA06048F38191B6E97485`。

## 30. 修复 123AV 已登录误报（2026-07-22）

- 版本升为 `0.1.8`；修复界面同时显示“未登录”和已识别账号的问题。
- 根因是 123AV 页面在登录后仍可能保留隐藏密码输入框，旧检测把任意密码框都当作退出登录证据。
- 登录状态改为以可见、有效的账号标识为准；访问验证页仍保持最高优先级，没有账号标识时仍按未登录处理。
- DOM 检测只读取可见按钮和可见密码输入框，隐藏登录表单不再触发退出登录；不读取密码值、Cookie、Local Storage、Session Storage 或页面 HTML。
- 新增账号标识与旧登录标记冲突的状态机回归，并在真实 Electron DOM 中加入隐藏密码框复测；全量自动测试增至 64 项。
- 123AV 账号 DOM 冒烟、双站点桌面/416px UI、ASAR 修复内容和打包后独立临时数据目录启动均通过；未使用正式数据库，未执行真实 123AV 收藏。
- 新便携版：`dist\MissAV_Manager_v0.1.8.exe`；最后写入 `2026-07-22 12:58:21`；大小 `412,930,832` 字节；文件版本/产品版本 `0.1.8`；SHA-256 `BD37F1709B878A20B1B14D0286B134F163781DF28AC3A32AE65632EB1CEFBB14`。

## 31. MissAV 与 123AV 公平并行查询（2026-07-22）

- 版本升为 `0.1.9`；新批次不再等待整批 MissAV 结束，MissAV 与 123AV 查询队列会同时启动。
- 两站公平共享速度档位的总并发上限：“高速 4”是两站合计最多 4 路，“极限 16”是两站合计最多 16 路，不会因并行翻倍。
- 共享调度器在两站都有待办时轮转授予空闲槽，避免先启动的 MissAV 队列长期独占；一站完成后另一站自动使用全部空闲额度。
- 两站共用请求间隔和网络错误退避，任一站出现限流时都会降低总体压力；停止操作同时停止两站，运行中的只读查询回到队列。
- 新批次、断点续跑和 MissAV 网络错误产生的双站重试批次均使用并行调度；单独重跑 123AV 网络错误仍只运行 123AV。
- 总进度和预计时间按两站待查询总数汇总，结果表从批次创建起显示全部行；两站并发写回时以数据库中的最新四任务状态刷新，避免互相覆盖。
- 新增隔离 Electron 并行冒烟：8 条记录产生 8 次 MissAV 和 8 次 123AV 请求，123AV 在 MissAV 队列结束前启动且真实请求时间重叠；高速模式实测最大同时请求 3，未超过合计 4 路上限。
- 同一冒烟在并行处理中主动停止，确认 0 个遗留 `running` 查询、20 个待查询被安全保存；继续后批次完成且待查询归零。
- 64 项自动测试、桌面/416px UI、ASAR 并行代码和打包应用本体启动均通过；便携外壳首次解压与系统扫描约一分钟后，在独立临时数据目录创建 `v0.1.9` 数据库和日志。未写入正式数据库，未执行真实 123AV 收藏。
- 新便携版：`dist\MissAV_Manager_v0.1.9.exe`；最后写入 `2026-07-22 13:23:25`；大小 `412,935,529` 字节；文件版本/产品版本 `0.1.9`；SHA-256 `11BD8E50EE749A9074A0FF5273722F6508D165D3F5DFB4203F32DD5C30BEB5EB`。

## 32. MissAV 与 123AV 完全分开手动运行（2026-07-22）

- 版本升为 `0.1.10`；按用户最新决定撤销新批次自动双站并行，创建批次后默认不联网，由用户分别点击“开始 MissAV”或“开始 123AV”。
- 两张站点卡各自提供开始/继续和停止入口；未完成批次与批次详情也分别提供“继续 MissAV”和“继续 123AV”，不会再用一个按钮同时恢复两站。
- 同一时间只运行用户手动选择的一个查询站点，当前站点独享所选速度档位的并发；另一站保持排队，完成后批次继续显示为可恢复，直到两站查询均闭环。
- 进度和 ETA 只计算当前站点；站点卡在运行中显示完成数、预计剩余和当前并发，完成后显示本次条数、实际用时和每秒处理量。
- MissAV 网络错误重跑改为直接写回原批次且只运行 MissAV，不再建立并自动执行双站重试批次；123AV 重跑继续只影响本站。
- 分站点隔离 Electron 冒烟确认：8 条 MissAV 期间 123AV 请求为 0，手动启动 123AV 后才产生本站请求；高速 4 档两站分别达到 3 路实际并发，模拟 220ms 响应下各约 1 秒完成 8 条。
- 停止续跑冒烟确认 0 个遗留 `running`：先只恢复 MissAV 时 123AV 的 12 条任务全部保持排队，随后手动恢复 123AV 后批次完成。
- 优化 MissAV 单条结果持久化：番号、女优 Tag 和类型 Tag 改为一个事务、一次整库落盘，仍保持每条完成即持久化；最终 80 条隔离基准由 1,711ms 降至 147ms，数据库写入部分约快 11.6 倍（不代表真实联网总速度等比例提升）。
- 66 项自动测试、分站点隔离/停止续跑 Electron 冒烟、桌面/416px UI 和 ASAR 内容核验均通过；打包应用本体在独立临时数据目录成功创建窗口与日志。未读写正式数据库，未执行真实 123AV 收藏。
- 新便携版：`dist\MissAV_Manager_v0.1.10.exe`；最后写入 `2026-07-22 14:40:57`；大小 `412,942,565` 字节；文件版本/产品版本 `0.1.10`；SHA-256 `326A748074EFB3A3793B4AF7DD6B6D279CC8FCFCBD4A61D72C34971652B1DB37`。

## 33. 支持安全删除整个处理中批次（2026-07-22）

- 版本升为 `0.1.11`；运行控制的未完成批次、处理结果页和“番号库 -> 处理批次”详情均新增整批删除入口。
- 若批次正在查询，删除操作先发出停止信号，等待所有在途请求结束并完成最后一次安全写回，再删除批次，避免并发任务继续写入已删除记录。
- 删除事务一次清理 `processing_runs`、全部 `processing_run_items` 和全部 `processing_item_tasks`，不会留下孤立任务；确认框会预先显示明细和阶段任务数量。
- 删除前自动创建数据库备份；已经完成并写入永久 `codes`/标签关系的数据以及磁盘上的已导出文件继续保留，防止删除其他批次共用的长期数据。
- 删除完成后同步清空当前结果、选择状态、进度和批次引用，并刷新运行控制与批次列表；诊断日志记录批次 ID、删除数量和备份文件名，不记录原始输入全文。
- 数据库测试确认删除 2 条明细和 8 项任务后无孤立记录、备份存在且永久番号仍可查询；Electron 中途删除冒烟在运行 12 条中的第 3 条后发起删除，确认安全停机、备份生成、批次消失且界面归零。
- 66 项自动测试、桌面/416px UI、ASAR 删除代码和打包应用本体隔离启动均通过；测试未读写正式数据库。
- 新便携版：`dist\MissAV_Manager_v0.1.11.exe`；最后写入 `2026-07-22 15:07:40`；大小 `412,950,990` 字节；文件版本/产品版本 `0.1.11`；SHA-256 `F7C3F1B8A67A23D07F0A9D2633D3B6BCE971F22F12A60EDEFDCE1FBD3302E609`。

## 34. 双站点独立速度与 123AV 高速策略（2026-07-22）

- 版本升为 `0.1.12`；运行控制改为两个独立的七档速度面板，MissAV 与 123AV 的速度分别保存到本机设置和处理批次，续跑时分别恢复。
- 123AV 的 6、8、12、16 路档位新增“保持并发，错误后单独重跑”和“动态半速”策略；固定策略为默认值，网络错误不再把整个队列降为单线程或全局暂停。
- 动态半速策略在限流时最低保留所选并发的一半，例如极限 16 降为 8；MissAV 仍保留原有较保守的自适应保护，避免改变详情页和多候选查询语义。
- 123AV 网络错误继续写回原批次并显示独立重跑入口；固定策略不会盲目重试失败项，也不会影响 MissAV 配置和状态。
- 新增独立限流冒烟：32 条请求中模拟 5 条 HTTP 429，固定极限模式实际达到 16 路并发并在约 0.75 秒结束请求队列，27 条成功、5 条留待重跑；动态模式确认最低 8 路，未降到 1 路。测试使用临时数据库，未访问正式数据库或真实网站。
- 67 项自动测试、分站点手动启动/停止/续跑/删除冒烟及桌面/416px UI 均通过；ASAR 确认包含双速度配置、123AV 策略和数据库迁移代码，最终便携 EXE 使用独立临时数据目录成功创建窗口、日志和空数据库，未接触正式数据库。
- 新便携版：`dist\MissAV_Manager_v0.1.12.exe`；最后写入 `2026-07-22 15:42:37`；大小 `412,962,532` 字节；文件版本/产品版本 `0.1.12`；SHA-256 `CD3B53FA3EA30A92FAE6E8FC6B4625451126928D8EE7574350E9F5D49087192E`。

## 35. 独立可扩展站点工作台（2026-07-22）

- 版本升为 `0.1.13`；侧边栏新增“站点 / 查询与同步”，整套批次运行控制从番号输入页迁入独立页面。
- “处理”页只保留原文、文件导入、过滤后的番号和明确的“打开站点工作台”下一步按钮，不再与网站查询设置挤在同一页面。
- 站点工作台使用“一个网站一个独立容器”的结构：MissAV 与 123AV 分别聚合自己的运行状态、开始/继续/停止、速度、错误策略和专属设置；MissAV 输出目录也归回 MissAV 容器。
- 批次名称、统计、未完成批次、删除批次、总体进度和 ETA 作为站点工作台的公共批次层保留。以后新增第三方网站时，按相同站点容器接入，不需要修改输入页职责。
- UI 冒烟新增五个侧栏入口、输入页零站点控件、站点页两个独立容器和桌面/416px 堆叠布局断言；双站手动启动/停止/续跑/删除回归继续通过，正式数据库未被读写。
- 67 项自动测试、ASAR 站点页面内容和最终便携 EXE 隔离启动均通过；成品使用独立临时目录成功创建窗口、日志与空数据库，并在测试后关闭全部新进程。
- 新便携版：`dist\MissAV_Manager_v0.1.13.exe`；最后写入 `2026-07-22 16:09:04`；大小 `412,966,098` 字节；文件版本/产品版本 `0.1.13`；SHA-256 `24A2F6F2B7F482A04D2D1F7A551B013F96A97C9D95D929BF316313DBE93F5D4D`。

## 36. 单站分层工作台、123AV 分层限流与并行收藏（2026-07-22）

- 版本升为 `0.1.14`；站点工作台改为左侧二级目录与右侧单站详情，一次只显示 MissAV、123AV 或公共批次管理中的一个页面。新增网站时只增加目录项，不再横向堆叠完整配置卡。
- 正式日志只读分析确认，2830 条 123AV 极限 16 查询中的 1840 条异常主要是连续 HTTP 429，而非普通断网。新增默认“分层收尾”策略：首轮保留所选高并发，最近 12 次中至少 6 次为 429/访问限制时才降档并冷却；极限档按 16→8→4 收敛，错误项再以较低并发自动收尾，最低不降为 1 路。
- 保留“固定并发，错误后手动重跑”和“动态半速”两种旧策略；三种策略继续独立保存到本机设置和处理批次。HTTP `Retry-After` 响应会转换为最长 120 秒的冷却参考。
- 收藏瓶颈从日志确认是单个账号窗口逐页完整导航。账号收藏新增稳妥 1 路、快速 2 路（默认）、激进 3 路；每路使用独立隐藏工作窗口，共享同一持久登录会话，但仍不读取密码、Cookie 或浏览器存储。
- 收藏详情页去掉固定 500ms 等待，改为最多 5 次、每次 120ms 的可见 DOM 就绪探测；结果表刷新改为 120ms 合并刷新。已收藏仍不点击，点击后仍必须看到“已保存”，任何不明确状态继续进入 `verify_required`。
- 任一收藏窗口检测到未登录或访问验证时停止派发新项目并打开主账号窗口；已在途项目完成写回。渲染层隔离冒烟确认 2 路配置真实同时发出两个收藏调用，三个任务全部成功且使用两个不同工作槽。
- 67 项自动测试通过；极限策略冒烟确认固定模式仍达到 16 路，分层模式在 429 波次后为 8 路、收尾为 4 路；账号幂等冒烟和桌面/416px 单站 UI 冒烟通过。所有测试使用临时数据库和模拟页面，未写入正式数据库，未执行真实 123AV 收藏。
- 新便携版：`dist\MissAV_Manager_v0.1.14.exe`；最后写入 `2026-07-22 16:47:43`；大小 `412,989,459` 字节；文件版本/产品版本 `0.1.14`；SHA-256 `9071CED9F2109D8BEC08540EBACF34846A1846ADEE069767F8A29EF71B15F2F0`。ASAR 内容核验通过；最终便携外壳的隔离启动请求因桌面权限审批服务连续超时未实际执行，没有留下应用进程，也未访问正式数据库。

## 37. 123AV 高速主轮与错误收尾解耦（2026-07-22）

- 版本升为 `0.1.15`。正式日志对比确认，同批数据使用智能 3 路仅约 1.63 条/秒，而固定 8 路原始吞吐约 14.87 条/秒；长耗时主要来自低档位、同一 429 波次连续降档和全局冷却，并非每个 123AV 页面本身必须等待数秒。
- 推荐“高速收尾”改为主轮始终保持用户所选并发。主轮中的 HTTP 429 只标记对应番号，不会让后续成功番号从 16 路连续降为 8 路、4 路，也不在主轮插入 12～20 秒全局暂停。
- 主轮结束后只读取本轮 `network_error` 项进行收尾；第一轮使用一半并发并短暂冷却，仍失败的项目再使用四分之一并发。成功、未找到项目不会重复查询。
- 新安装的 123AV 默认档位由智能 3 路改为飞速 8 路；已有用户明确保存的站点档位和批次档位不会被覆盖。界面明确区分高速收尾、只跑一轮和动态半速。
- 隔离速度冒烟中，32 条固定 16 路主轮约 0.8 秒完成并留下 5 条模拟 429；高速收尾场景总计只追加错误项请求，约 3.8 秒恢复为 32/32 成功，最大并发 16。67 项自动测试、双站独立启动/停止/续跑/删除与桌面/416px UI 冒烟通过；未访问真实网站或正式数据库。
- 新便携版：`dist\MissAV_Manager_v0.1.15.exe`；最后写入 `2026-07-22 17:39:43`；大小 `413,099,711` 字节；文件版本/产品版本 `0.1.15`；SHA-256 `5DC75895BE2903D0897CF7A1646BBCDECA5DEDA8D2912A99A1B2331BE2198D2E`。ASAR内容及最终便携外壳隔离启动通过，测试进程全部关闭，未访问正式数据库。

## 38. 123AV 全局速率调度、详情优先与永久查询缓存（2026-07-22）

- 版本升为 `0.1.16`。对正式运行日志做只读时序分析后确认，123AV 限制的是时间窗口内的请求速率，不是简单的 worker 数。高并发会在约 49 个响应后进入约 10.2～10.6 秒的成片 HTTP 429，有效吞吐没有随 8/16 路继续上升。
- 123AV 七档速度改为 1.0/2.4/3.2/3.5/3.7/3.9/4.1 请求每秒的独立全局调度。工作路数仍用来覆盖网页延迟，但所有发送都按最小间隔和 10 秒滑动窗口平滑排队，不再瞬间爆发。
- 首个 429 立即打开全队列断路器：优先遵循 `Retry-After`，否则默认冷却 10.5 秒；同一波在途响应不会反复延长默认冷却。恢复时将 RPS 降为原来的 82%，每 120 条稳定响应回升 0.1 RPS。界面 ETA 区域实时显示当前 RPS 和冷却剩余时间。
- 查询路由改为标准详情地址优先，只在详情页不匹配时转入精确搜索补查。真实只读 A/B 中，普通番号 1 次请求成功，两个 `-uncensored-leaked` 后缀例外均在第 2 次搜索补查成功，全部 HTTP 200，未执行账号收藏。
- 新增 `site_lookup_cache`：成功结果缓存 365 天，未找到缓存 30 天；网络错误、人工核验和账号操作不缓存。缓存与批次明细在同一 SQLite 事务中更新，删除批次不会删除永久查询缓存。
- 修复大批量本地写盘瓶颈：123AV 不再每条用“运行中 + 结果”两次完整导出 sql.js 数据库，改为内存事务更新后每 100 条或 30 秒检查点，队列停止、轮次结束和应用退出时强制落盘。异常崩溃最多只会安全重查最近一小段只读任务。
- 123AV 公开查询使用自己的持久 Chromium 会话和当前 Electron User-Agent；它与 123AV 账号收藏会话分开，不读取密码、Cookie、Local Storage 或 Session Storage。
- 69 项自动测试通过。速度冒烟确认极限档请求平滑间隔、单波 429 断路/收尾、详情页零搜索请求和 32 条第二批 132ms 全缓存命中；双站手动分离、停止续跑、运行中删除及桌面/416px UI 回归通过。
- 新便携版：`dist\MissAV_Manager_v0.1.16.exe`；最后写入 `2026-07-22 18:39:38`；大小 `413,002,245` 字节；文件版本/产品版本 `0.1.16`；SHA-256 `EBF893C8D53B7E46194D3B57DFFD6944E56EE2387A99992AFD7FED4314BD187A`。ASAR 关键代码、应用本体与最终便携外壳隔离首启均通过，测试进程全部关闭，正式数据库未被测试写入。

## 39. Raindrop 官方 API 直接同步（2026-07-22）

- 版本升为 `0.1.17`；侧边栏新增独立“同步 / Raindrop”页面，站点页继续只管理 MissAV 与 123AV，不把第三项配置堆入同一工作界面。
- 支持验证和保存个人 Test Token / OAuth Access Token。令牌只由 Electron 主进程读取，并通过系统 `safeStorage` 加密写入用户数据目录；渲染层、本地 SQLite 和运行日志都不保存令牌明文。
- 同步前选择处理批次和远端 Collection。未知链接调用官方 URL exists 接口并按最多 100 条一组核对；已存在的远端书签更新，不存在的创建，本地 payload 与上次一致的直接跳过。
- 新增 `remote_sync_records` 幂等映射表；同步任务状态与远端 ID、Collection、payload hash 在同一内存事务中写回，并每 20 条检查点落盘。程序中断或写入网络结果不明确时进入 `verify_required`，下次先核对 URL，不能盲目重复创建。
- 主进程为 Raindrop API 使用独立持久网络会话和约 550ms 的全局最小请求间隔，遵循 `X-RateLimit-*` / `Retry-After`，HTTP 429 只阻塞 Raindrop 队列，不影响 MissAV 或 123AV。
- 77 项自动测试通过；新增 payload 安全校验、Collection 层级、URL 映射、速率头解析、远端记录原子写回、中断恢复和日志凭据脱敏测试。桌面与 416px UI 冒烟使用临时数据库及模拟 API，验证 3 条创建、1 条更新、4 条成功落库、进度 100%、完成按钮禁用和令牌输入清空；未访问正式 Raindrop 账号或正式数据库。
- 新便携版：`dist\MissAV_Manager_v0.1.17.exe`；最后写入 `2026-07-22 20:46:50`；大小 `413,055,532` 字节；文件版本/产品版本 `0.1.17`；SHA-256 `CFBC4AD855D5C98887BEEC0CDB888AB42B732489D77C0EA557488F0E9368A2AB`。ASAR 关键代码、打包应用本体和最终便携外壳隔离首启均通过；测试子进程与临时目录已清理，未访问正式数据库或正式 Raindrop 账号。

## 40. Raindrop 同步预览说明与大表格（2026-07-22）

- 版本升为 `0.1.18`。远端 URL 命中提示从简略的“远端已存在 #ID，将更新”改为“官网已有同一链接（书签 ID #ID）”，并明确说明会更新标题、Tags，同时放入当前选择的 Collection；新建项也明确写明目标是当前 Collection。
- 桌面端同步明细默认高度扩大为约 52% 视窗且不少于 420px；表格下边缘支持垂直拖动，滚动条加宽，表头固定，查看大量记录时不再被狭小滚动框限制。
- 新增“展开表格 / 还原表格”按钮。展开后明细成为独立的大面板，`Esc` 可退出；离开同步页面时自动还原，避免遮挡其他页面。416px 窄屏维持页面宽度，只有表格内部按需横向滚动。
- 77 项自动测试及桌面/416px UI 冒烟通过；桌面实测默认表格高 420px，展开后使用固定大面板并可正常还原。测试使用临时数据库和模拟 API，未访问正式数据库或正式 Raindrop 账号。
- 新便携版：`dist\MissAV_Manager_v0.1.18.exe`；最后写入 `2026-07-22 21:15:21`；大小 `413,059,149` 字节；文件版本/产品版本 `0.1.18`；SHA-256 `37693D6ED76A68E5C51C6364062AFBBB0767770DB0608F0DF800E54AEDAE64EA`。ASAR 关键代码核验通过；最终便携外壳在独立临时用户目录中成功创建窗口、版本日志和空数据库，测试进程与临时目录已清理，未访问正式数据或正式账号。

## 41. 123AV 高上限动态 RPS（2026-07-22）

- 版本升为 `0.1.19`。确认并移除了 `v0.1.16` 遗留的隐藏瓶颈：即使界面选择极限 16，新速率上限仍会被旧档位的 244ms 最小间隔压回约 4.1 RPS。现在工作路数只负责覆盖页面延迟，真实请求间隔完全由独立 RPS 控件决定。
- 123AV 页面新增“实际请求速率”分区，与并发、网络错误收尾、收藏脚本速度分别配置。支持自动探速或固定速率，滑杆范围 1–24 RPS，提供 4/8/12/16/20/24 快捷按钮；自动模式默认上限 16 RPS，运行中可以直接调整，无需停止批次。
- 自动探速首次从 5 RPS 开始，以后读取本机学到的稳定值，最低不会低于 3 RPS。无 429 时按成功数量和稳定时长逐级探高；首个 429 暂停全队列、RPS 回退到约 80%，本轮上限压到失败值的约 90%，同一波在途 429 不重复降速。429 后需要更长稳定区间才再次升速。
- 自动学到的稳定 RPS 保存在本机；调速方式和用户上限写入处理批次，停止或重启后可恢复。运行 ETA 同时显示工作路数、当前/上限 RPS 和实际番号/秒，日志记录起始值、探速、断路、最终 RPS 和 429 次数。
- 专项模拟确认自动模式可从 5.0 升到 5.5 RPS，模拟 429 后回退到 4.4 且本轮上限为 4.95；固定 12 RPS 时 32 条详情请求约 3.24 秒完成，最小启动间隔 84ms、最大同时 3 请求，确认超过旧 4.1 RPS 且没有瞬时爆发。78 项自动测试及桌面/416px UI 回归通过，全部使用模拟网页和临时数据库。
- 新便携版：`dist\MissAV_Manager_v0.1.19.exe`；最后写入 `2026-07-22 21:42:58`；大小 `413,077,152` 字节；文件版本/产品版本 `0.1.19`；SHA-256 `20877914AC90FB672E98C6FD6619650BE5C52615FF9EA1F114648F16CD50A2A6`。ASAR 关键代码核验通过；最终便携外壳在独立临时目录中成功创建窗口、版本日志和空数据库，测试进程与临时目录均已清理，未访问正式数据或正式账号。

## 42. 123AV 纯详情候选查询（2026-07-22）

- 版本升为 `0.1.20`。根据用户手动核验，抽取样本中 8 个标准详情链接可以直接打开；进一步定位到正常详情页包含相关影片的 `card__link`，旧分类顺序因此把真实详情页误判成搜索页并重复补查。现在 URL 的 `/cn/v/` 详情路径优先于页面内相关卡片证据。
- 123AV 批量查询彻底移除搜索页回退，只尝试详情地址。高速 6～16 路档位检查标准地址、`-uncensored-leaked` 和 `-uncensored-leak`；稳妥、智能和高速档继续尝试 `-chinese-subtitle`、`-english-subtitle`、`-uncensored`、`-leaked` 等全部已知详情变体。
- 对正式运行日志只读统计 555 个过去由搜索补查成功的链接：552 个（99.46%）实际仍是标准详情地址，3 个（0.54%）为 `-uncensored-leaked`，未观察到其他后缀。该统计只读取日志，没有修改正式数据库或访问账号。
- 78 项自动测试通过。123AV 专项模拟验证标准地址 404 后第 2 次直接命中 `-uncensored-leaked`，总搜索请求为 0；固定 12 RPS 的 32 条详情请求约 3.26 秒，最大同时 3 请求。桌面与 416px UI 回归通过，均使用模拟网页和临时数据库。
- 新便携版：`dist\MissAV_Manager_v0.1.20.exe`；最后写入 `2026-07-22 22:10:49`；大小 `413,078,346` 字节；文件版本/产品版本 `0.1.20`；SHA-256 `BF59A8A8819A39E188B528C7BEF1619A50849BE4AF4FFC9EF4F4287241811023`。ASAR 关键代码和包元数据核验通过；最终便携外壳在独立临时目录中成功创建窗口、`v0.1.20` 日志和空数据库，5 个测试派生进程与临时目录均已清理，未访问正式数据库或正式账号。

## 43. 双站硬超时与同级动态调速（2026-07-22）

- 版本升为 `0.1.21`。只读分析正式运行日志确认：123AV 最新长批次没有 HTTP 429，但大量成功响应占用工作路 55～61 秒；MissAV 最新运行也出现连接重置、Cloudflare 页面和类似的长占路。界面选择 16 路时，实际工作路因此被慢正文逐渐占满。
- 新增 `src/networkTransport.js`，查询改用 Electron `ClientRequest` 的显式硬总时限。超时会同时中止请求与正在读取的响应正文；本地慢正文冒烟在约 1.01 秒终止，普通请求、持久 Chromium 会话和重定向均通过。
- Electron 请求超时、连接重置或其他运行期网络失败默认直接返回本站异常队列，不再暗中改用 Node 发第二次请求。Node 实现保留为显式兼容能力，但 MissAV/123AV 查询默认不启用，避免一次失败变成两次等待。
- MissAV 获得与 123AV 同级的独立实际请求速率面板：自动探速/固定速率、1–32 RPS 上限、4/8/12/16/24/32 快捷值、运行中调整、当前 RPS 与实际番号/秒。123AV 上限也由 24 提升到 32 RPS。
- 两站分别保存自己的速率模式、上限和学习值，并随批次持久化；旧数据库通过新增 `missav_speed_policy`、`missav_rate_mode` 和 `missav_rate_cap` 列兼容升级，不修改正式数据内容。
- 自动模式不再只看 429。最近响应的 p95 延迟、硬超时、ECONNRESET、Cloudflare/验证码页和错误率都会触发本站独立平滑回退；延迟恢复并达到稳定样本数后才继续探高。
- MissAV 新增“稳定自适应 / 只跑一轮保持工作路 / 动态半速”三种网络错误策略。固定策略适合用户已验证的高速环境；动态半速在 6～16 路高档发生异常时最低保留一半工作路。
- 79 项自动测试通过。隔离调速冒烟验证 MissAV 5 秒 p95 延迟会从 8 降至 6.56 RPS，123AV 自动探速、429 断路、纯详情候选、分层收尾和缓存保持正常；分站点手动启动、停止续跑和运行中删除回归通过。桌面/416px UI 确认两个网站仍一次只显示一个独立容器。
- 新便携版：`dist\MissAV_Manager_v0.1.21.exe`；最后写入 `2026-07-22 23:18:00`；大小 `413,104,125` 字节；文件版本/产品版本 `0.1.21`；SHA-256 `C0941081BC96514CE70210F5E3EA1A53011383D16FE13CB0AD2D0E7A7005FD9A`。ASAR、打包应用本体和最终便携外壳隔离首启均通过；未访问正式数据库、正式账号或执行真实收藏。

## 44. 123AV 查询/收藏分层与流式自动收藏（2026-07-23）

- 版本升为 `0.1.22`。123AV 站点详情内部拆成“番号查询”和“账号收藏”两个子页面；查询速度、网络异常与账号登录、收藏并发、收藏进度不再堆在同一屏。
- 新增批次级“查询命中后自动收藏”。开始 123AV 查询前只确认一次账号和预计数量；随后每个命中项立即流式进入独立收藏队列，不等待整批查询结束。未开启时继续保留原有手动收藏流程。
- 查询和收藏使用独立停止信号。停止收藏不会改变只读查询的停止状态；登录失效或 CAPTCHA 只停止收藏，后来命中的项目保持为“待收藏”。删除运行中批次时会分别等待两类在途操作安全写回后再备份、删除。
- 收藏页新增独立完成数、实际条数/秒、进度条和 ETA。收藏并发仍为 1/2/3 个共享账号会话的隐藏窗口，并只对隐藏收藏窗口屏蔽图片、视频和字体；可见账号窗口不受影响。
- 收藏继续保持幂等：可见“已保存”直接成功；只有可见“保存”才点击；点击后未确认、中断或渲染异常统一进入 `verify_required`。应用重启不自动重放账号副作用。
- 查询速率文案改为“请求调度目标”，运行时分别显示实际网络请求完成速度、番号/秒和缓存命中数，避免把目标 RPS 当成真实吞吐。
- SQLite 批次新增 `av123_auto_favorite` 与 `av123_favorite_concurrency`，旧数据库通过兼容迁移补列。单条收藏写回改为按批次和位置精确读取，避免查询与收藏并发时用整批旧快照互相覆盖。
- 80 项自动测试通过。隔离 UI 冒烟确认 4 条查询与 4 条收藏流式重叠完成；单独停止收藏后 1 条在途收藏安全完成、3 条保持待收藏，而 4 条查询继续全部成功。桌面和 416px 窄屏、Raindrop 模拟同步、表格与批次页回归通过；未访问正式网站、正式账号、正式数据库或执行真实收藏。
- 新便携版：`dist\MissAV_Manager_v0.1.22.exe`；最后写入 `2026-07-23 10:48:32`；大小 `413,127,935` 字节；文件版本/产品版本 `0.1.22`；SHA-256 `5519FD6B546F8DC8996037202494B58C9A7A32A4B4E2FCA2AB8DA00143526332`。ASAR关键标记、打包应用本体和最终便携外壳隔离首启均通过，两次首启分别创建 `v0.1.22` 日志与 180,224 字节空数据库。

## 45. Telegram API / 官方导出双来源（2026-07-23）

- 版本升为 `0.1.23`。侧边栏新增独立“来源”页面，内部以二级目录分为“API 增量同步”和“官方导出文件”，一次只展示一种接入方式；不会把 Telegram、站点处理和 Raindrop 同步控件堆在同一页面。
- Telegram 用户 API 使用 `teleproto` 读取当前账号自己的 Saved Messages。登录支持手机号、Telegram 验证码、可选两步验证和邮箱步骤；`api_hash` 与 StringSession 只在主进程中使用，并由 Windows `safeStorage` 加密保存。手机号、验证码、两步验证密码和完整消息正文不写数据库或日志。
- 官方导出入口支持 Telegram Desktop 的 `result.json`、单个或多个 `messages.html`，以及完整导出目录。目录优先识别官方 JSON，其次从 `export_results.html` 定位 Saved Messages；只读取文本、链接、文件名和说明，不复制媒体。
- API 与导出文件统一转换成消息信封，优先按“账号 + Saved Messages + 消息 ID”强去重，缺失账号时按内容摘要补充去重。同一文件重复导入，或先导入文件后再由 API 读到，只记录历史重复，不再次产出番号；内容编辑会更新原消息引用并重新产出变化后的番号。
- 新增 `telegram_sources`、`telegram_message_refs`、`telegram_import_runs`。数据库只保存来源、消息 ID、日期、SHA-256 摘要和提取番号，不保存完整 TG 正文。导入结果先显示消息数、新增、重复、更新和一行一个的新番号，由用户点击“送入番号处理”；不会自动创建批次或启动 MissAV / 123AV。
- API 大批量同步支持 500 / 1000 / 5000 / 20000 条单次上限、最早日期和断点回看。若新消息超过本次上限，会同时保存向旧消息继续的游标与本轮目标最高 ID；只有整段补齐后才推进最高检查点，中途停止或分批读取不会跳过中间消息。
- 89 项自动测试通过，其中 Telegram 专项覆盖 JSON / HTML 解析、API 授权凭据不外泄、跨来源去重、编辑更新、加密会话恢复和多页游标。Telegram 专项桌面/416px UI 冒烟与全量 MissAV、123AV、Raindrop、表格、批次 UI 回归通过，均使用模拟账号和临时数据库。
- 生产依赖经 npm 官方端点审计为 0 个已知漏洞；审计发现的打包工具链 `fast-uri 3.1.3` 已升级为 `3.1.4`，并确认修复版本进入最终 ASAR。
- 新便携版：`dist\MissAV_Manager_v0.1.23.exe`；最后写入 `2026-07-23 12:10:25`；大小 `417,031,795` 字节；文件版本/产品版本 `0.1.23`；SHA-256 `56BF6E98808579ACB7D678AAF6F95F2BD4A7E6DFBEC74FA0CA4960BCA964F26E`。ASAR 关键标记、打包应用本体和最终便携外壳隔离首启均通过，两次首启分别创建 `v0.1.23` 日志和 217,088 字节空数据库；未登录真实 Telegram、未访问正式数据库、正式网站账号或执行真实收藏。

## 46. 最多 5 个指定 Telegram 群组增量来源（2026-07-23）

- 版本升为 `0.1.24`。根据真实工作流，Telegram 用户 API 不再以 Saved Messages 作为界面来源，改为连接账号后列出群组、超级群组和频道，由用户明确选择最多 5 个增量来源。
- 首次保存群组时记录它当时的最新消息 ID，同时作为基线和初始检查点；正常同步只读取基线之后的新增消息，第一次运行不会重新扫描过去历史。
- 每个群独立保存基线、最高检查点、跨页游标、本轮目标、最近同步状态和错误。可同步全部来源或只同步一个群；停止当前群后不会继续启动后续群。
- 默认回看只发生在首次绑定基线之后，用于发现后来编辑过的消息。填写“补查起始日期”时才允许临时越过基线回补导出与 API 接入之间的空档，结果继续经过统一消息历史和永久番号查重。
- 移除来源只把该群从白名单停用，不删除消息指纹、已提取番号或原游标；以后重新加入时沿用原检查点，不以新的最新消息覆盖旧进度。
- Telegram Desktop JSON 解析改为识别群组、超级群组和频道容器并忽略账号级导出中的普通私聊与 Saved Messages；单群顶层导出、HTML、多文件和目录入口继续支持。导出负责旧历史，API 负责今后增量。
- 群组访问 Peer 只保存在主进程内存；渲染层只接收群名称、类型、ID、公开用户名和管理状态，不接收访问哈希。程序仍不发送、转发、删除、标记已读或保存完整消息正文。
- 自动测试增至 90 项，新增群组发现过滤、来源上限、首次基线不扫历史、重新加入保留游标和群组独立跨页断点。Telegram 专项桌面/416px UI 使用模拟账号与临时数据库通过；未访问正式 Telegram 或正式数据库。
- 新便携版：`dist\MissAV_Manager_v0.1.24.exe`；最后写入 `2026-07-23 12:48:20`；大小 `417,062,097` 字节；文件版本/产品版本 `0.1.24`；SHA-256 `EBAFD6E21A965CBA4DED819C7F1A22309CF1D0CA77389B2CD6E71247658B40E8`。ASAR 群组白名单、首次基线和独立游标标记通过；打包应用本体与最终便携外壳均在隔离目录成功创建 `v0.1.24` 日志和 221,184 字节空数据库，未登录真实 Telegram、访问正式数据库或执行真实账号操作。

## 47. Telegram 扫码登录与自动重连（2026-07-23）

- 版本升为 `0.1.25`。Telegram 授权区改为“显示登录二维码”主入口，手机号验证码折叠为备用入口；首次仍需用户自己的 `api_id` 和 `api_hash`。
- 使用 `teleproto` 官方 `signInUserWithQrCode` 流程生成短时登录 token，并由本地 `qrcode 1.5.4` 转成二维码图片。二维码约 30 秒过期后自动刷新；手机端从“设置 → 设备 → 连接桌面设备”扫码确认。
- 二维码授权继续支持可选两步验证。取消扫码使用 `AbortController` 真正停止后台授权，不保存半成品 StringSession；手机号授权取消也统一清理等待步骤。
- 二维码 token 只存在于主进程短时内存与当前二维码图片中，不写 SQLite、日志或磁盘；日志清洗额外屏蔽 `tg://login?token=...`。API 凭据和成功会话仍只经 Windows `safeStorage` 加密保存。
- APP 启动后会自动连接有效的加密会话，不再要求日常手动点“连接已保存会话”；自动连接只建立授权通道，不主动同步或读取群消息，仍需进入来源页手动点击同步。
- 授权操作增加前端状态代次保护：刚进入来源页就快速点击扫码时，较早发出的账号状态刷新不能再晚到并覆盖二维码；专项 UI 冒烟使用延迟状态响应复现并验证该竞态。
- 已保存会话失效时可在 API 凭据输入框留空，扫码或手机号登录会复用加密保存的 `api_id` / `api_hash`；切换不同 API 凭据时仍可显式重新填写。
- 新增二维码生成、2FA、token 不外泄和取消授权测试；全量自动测试增至 92 项。Telegram 专项桌面/402px UI 验证自动重连、二维码倒计时、备用入口折叠和窄屏无横向溢出；全量 MissAV、123AV、Raindrop、表格和批次 UI 回归通过。
- 生产依赖通过 npm 官方安全审计，0 个已知漏洞。ASAR 包内容、打包应用本体和最终便携外壳均通过隔离首启；没有登录真实 Telegram、访问正式数据库、正式网站账号或执行真实收藏。
- 新便携版：`dist\MissAV_Manager_v0.1.25.exe`；最后写入 `2026-07-23 13:26:13`；大小 `418,621,860` 字节；文件版本/产品版本 `0.1.25`；SHA-256 `8F4945F85B07CB3D22B15C926452F68A280F65BB233150FA0DBBD280B04F9DDF`。

## 48. Telegram Bot API 增量入口（2026-07-23）

- 版本升为 `0.1.26`。来源页增加“机器人增量”，并保留“个人账号 API”和“官方导出文件”；默认打开机器人入口，不再要求先登录 `my.telegram.org` 才能开始增量工作。
- Bot Token 只在主进程使用，经 Windows `safeStorage` 加密保存到独立文件；状态 IPC、SQLite 和运行日志都不返回明文。日志清洗新增 Bot Token 和带 Token 的 `api.telegram.org` URL 屏蔽。
- 连接只调用只读 `getMe`；消息同步只调用 `getUpdates`。机器人不能枚举全部群，界面引导用户在最多 5 个自建群中发送识别消息，再从待处理更新中发现并保存来源。关闭隐私模式后需要重新加入群；管理员机器人也可接收全部群消息。
- Bot API 使用机器人级全局 `update_id`，因此已选群组一起同步，不提供会错误推进全局队列的单群同步。每批最多读取 100 条；所有已选群消息先完成 SQLite 去重与落库，再保存全局游标，下一请求才确认前一批。异常重放只计历史重复，不漏消息。
- 首次保存把发现时最新消息作为基线，识别消息不进入番号结果。未选群更新会安全忽略并推进全局游标；移除来源不删除消息指纹、番号和原进度。
- 机器人同步保持完全手动，不会自动创建批次、启动 MissAV/123AV、收藏账号或同步 Raindrop。停止按钮在当前最多 100 条安全落库后结束。
- 若机器人已经由其他服务设置 Webhook，APP 不擅自删除远端配置，而是提示换新机器人或先在原服务停用。Telegram 未取更新最长保留约 24 小时；本地写入后永久保留，电脑长期关闭时继续使用官方导出文件补齐。
- 自动测试增至 96 项，覆盖 Token 格式/清洗、群消息和频道消息转换、私聊忽略、隐藏链接、加密存储边界、传输错误脱敏、Bot/用户 API 白名单隔离和数据库不保存正文。Telegram 专项隔离 UI 同时验证机器人、个人账号二维码 API、官方导出、跨来源重复和 402px 无横向溢出。
- 全量 MissAV、123AV、账号收藏、Raindrop、批次、表格和手机布局 UI 回归通过。ASAR Bot/二维码/数据库关键标记全部存在；打包应用本体与便携外壳分别在隔离目录成功创建 `v0.1.26` 日志和 221,184 字节空数据库，没有使用真实 Bot Token、正式数据库或网站账号。
- 新便携版：`dist\MissAV_Manager_v0.1.26.exe`；最后写入 `2026-07-23 14:29:27`；大小 `418,668,548` 字节；文件版本/产品版本 `0.1.26`；SHA-256 `57574EC10436C6A2815B6ED188414659C91BA6FE11CCF38F6D9F098570278BD4`。

## 49. 123AV 账号收藏快速确认与自动第二轮（2026-07-23）

- 版本升为 `0.1.27`。正式日志中一批 2 路收藏处理 49 条耗时约 159 秒：30 条发生点击，其中 20 条因为按钮在最长约 10 秒内没有刷新成“已保存”而进入 `verify_required`；不少记录后来重新打开时已明确是“已保存”，确认等待是主要吞吐瓶颈。
- 点击后的页面内确认由默认 `20 × 500ms` 缩短为 `4 × 400ms`。首轮未及时看到“已保存”时先安全写入 `verify_required`，不继续占住当前工作窗口。
- 收藏运行器增加全局第 2 轮：主轮完成（流式自动收藏则在查询输入关闭）后，把首轮状态不明确、导航网络错误和收藏按钮暂不可用的记录重新打开一次。重新进入页面仍先检查目标番号、账号和按钮；已是“已保存”直接成功，明确显示“保存”才允许再次点击。
- 自动重跑最多一轮，不会无限循环。第 2 轮仍不明确的记录继续保留为“需核对远端”；登录失效或验证码仍立即暂停整条收藏队列；若不确定状态无法写入 SQLite，队列安全停止。
- 收藏速度增加可选“冲刺 4 路”；默认仍为快速 2 路。四个隐藏窗口共享应用内持久登录会话，继续屏蔽图片、视频和字体，并额外屏蔽 Ping；4 路更可能触发访问验证，界面明确提示风险。
- 进度按唯一记录计算，不把第二轮重复计入总数；运行面板和日志单独显示当前轮次、实际尝试次数、自动重跑数量和最终待核对数量。
- 自动测试增至 97 项。隔离 UI 用一条首轮 `post_click_timeout`、第二轮 `already_saved` 的模拟记录验证：4 条记录最终全部成功、总进度仍为 4/4、实际尝试 5 次、自动重跑 1 条、只启动两轮；独立停止仍不影响 123AV 查询。
- 全量 UI、账号 DOM 冒烟、ASAR 包内容和便携构建通过。应用本体与便携外壳分别在隔离数据目录创建 `v0.1.27` 日志和 221,184 字节空数据库，没有连接真实账号。新便携版：`dist\MissAV_Manager_v0.1.27.exe`；最后写入 `2026-07-23 16:05:04`；大小 `418,675,539` 字节；文件版本/产品版本 `0.1.27`；SHA-256 `17A63452735B0BE4D22CEECC8240EFC45B7F19FC89E6EAF91F82CCF0177BD4DF`。

## 50. 123AV 收藏全局节流与 Error 1015 自动恢复（2026-07-23）

- 版本升为 `0.1.28`。正式日志确认 2 路收藏不到 10 条便遇到 Cloudflare Error 1015；随后改为 1 路，112 秒内尝试 41 页、成功完成 25 条后仍再次触发。因此根因是同一 IP/会话在滚动时间窗口中的累计页面启动量，而不是单纯并发窗口数。
- 1～4 个收藏窗口改为共享一个全局详情页许可闸门。基础间隔为 1/2/3/4 路分别 3.0/2.6/2.5/2.4 秒；并发窗口只遮住页面加载延迟，不再把详情页爆发量倍增。
- 每启动 15 个详情页主动休息 15 秒。界面进度和 ETA 计入全局间隔、分段休息和正在进行的冷却，并显示打开页数、1015 次数、“分段休息中”或“1015 冷却中”。
- 账号 DOM 单独识别 `Error 1015` / `You are being rate limited`。该状态归为不需人工的 `network_error + rate_limited`，不再当作掉线或 CAPTCHA，也不会弹出账号窗口。
- 遇到 1015 时，当前项进入第 2 轮，所有工作窗口至少冷却 120 秒；全局启动间隔扩大为当前值的 1.5 倍并持久化。每连续 50 个非限速结果才小幅回收 250ms，并且不低于本档基础间隔。
- 停止会唤醒正在节流/冷却的工作窗口；还未获得页面许可的记录不会被写成 `running`。查询队列与收藏队列的独立停止语义保持不变。
- 自动测试增至 98 项，新增 1015 分类回归。隔离 UI 冒烟确认多窗口页面启动间隔不小于 2.4 秒；模拟 1015 后等待冷却、自动第 2 轮并恢复成功；节流等待中停止不遗留运行状态。全部使用临时数据库和模拟账号，未执行真实收藏。
- ASAR 关键标记、打包应用本体和最终单文件外壳隔离首启通过，均创建 `v0.1.28` 日志和 221,184 字节空数据库。新便携版：`dist\MissAV_Manager_v0.1.28.exe`；最后写入 `2026-07-23 16:38:03`；大小 `418,683,914` 字节；文件版本/产品版本 `0.1.28`；SHA-256 `8A5B3D5DCD100D51688ADC61E337402F021B71CE8085020B617ECA4CB3D19438`。

## 51. 123AV 收藏迁移到本地 Chrome 扩展桥（2026-07-23）

- 版本升为 `0.1.29`。123AV 公开番号查询继续由 APP 的独立网络会话执行；只有账号收藏改用用户本机正在使用的 Chrome，以复用现有登录环境和浏览器访问特征。
- 新增 Manifest V3 扩展。首次使用时 APP 把扩展文件复制到用户数据目录并打开文件夹；用户在 `chrome://extensions` 开启开发者模式、加载已解压扩展，然后把 APP 自动复制的配对码粘贴到扩展弹窗。Chrome 不允许普通桌面程序静默安装本地扩展，因此这一步只需人工完成一次。
- APP 只绑定 `127.0.0.1`，使用 256 位随机密钥和 Bearer 认证。密钥通过 Windows `safeStorage` 加密保存，不进入运行日志；扩展任务和返回结果都经过白名单清洗。
- 扩展只读取 123AV 页面中可见的账号数字、番号、标题和“保存/已保存”按钮状态；不读取密码、Cookie、Local Storage、Session Storage 或整页 HTML。已收藏直接跳过，点击后必须看到“已保存”才成功。
- v0.1.28 的全局启动间隔、每 15 页强制休息、整队 1015 冷却和跨启动学习降速不再进入实际收藏路径。1～4 路改为本地 Chrome 标签页并发；1015、导航异常或点击后状态不明只影响当前项目，并在主轮结束后最多重跑一次。
- 登录失效或验证码仍暂停收藏队列，但不影响 123AV 公开查询；停止收藏仍等待在途页面写回，未开始项保留为待收藏。APP 重启后原 `running` 副作用状态继续按 `verify_required` 恢复。
- 新增本地桥认证、任务投递、结果清洗和未配对拒绝测试；自动测试增至 101 项。全量站点工作台/结果表/自动收藏/独立停止/Raindrop/Telegram/416px UI 冒烟通过，全部使用临时数据库和模拟账号，未执行真实收藏。
- ASAR 确认包含本地桥、MV3 扩展和 FC2/普通番号精确识别。恢复后的最终便携版隔离首启成功创建 `v0.1.29` 日志、加密桥接密钥和 221,184 字节空数据库；测试进程按唯一标记清理。当前便携版：`dist\MissAV_Manager_v0.1.29.exe`；最后写入 `2026-07-23 22:07:18`；大小 `418,715,728` 字节；文件版本/产品版本 `0.1.29`；SHA-256 `8CE99677ABF721FF56DFE51E8DB5B7D4ECF58EE5FC4E400336522E68C91EE45C`。

## 52. 123AV 三种收藏方式与站点级串行（2026-07-23）

- 版本升为 `0.1.30`。账号收藏页只保留 `Chrome 扩展`、`APP 内执行器`、`仅导出` 三种方式，不恢复 Codex 接管、任务包、女优关注或执行器对比实验。
- Chrome 与 APP 远端收藏统一固定 1 路。渲染层只创建一个工作协程；主进程不再保留四槽窗口/Promise 队列，而是所有 123AV 收藏共用一条串行队列并固定 `workerId=0`，防止界面异常造成同站并发。
- 调度边界改为按网站隔离：123AV 查询与 123AV 收藏不重叠；批次级自动收藏只在 123AV 查询全部结束后启动。MissAV 查询使用另一站队列，可与 123AV 收藏同时运行。
- APP 内执行器遇到普通网络异常、Error 1015 或状态不明时，整条收藏队列暂停 10 秒再自动继续；同一项最多进入第 2 轮一次。登录失效和 CAPTCHA 仍要求用户处理，不盲目续点。
- “仅导出”生成一行一个番号的 TXT 和包含详情 URL、收藏状态、批次与位置的 CSV，不访问账号，也不修改远端状态。
- 新安装默认使用护眼淡绿主题和无背景图；已经保存过其他外观的用户保持原选择。
- 清理不参与产品运行的旧 `启动MissAV.bat`、VBS/快捷方式脚本、旧 123AV 调试样本、2.63 GB 隔离启动缓存/GPU 缓存/旧截图以及空的实验目录。正式数据库、附件、主题资源和当前可运行 EXE 未在清理阶段删除。
- 101 项单元测试、静态检查和隔离 Electron UI 冒烟通过。UI 模拟确认同站查询/收藏从未重叠、123AV 收藏最大并发严格为 1，同时 MissAV 查询与 123AV 收藏发生真实重叠；桌面与 416px 窄屏默认护眼绿且无背景图。ASAR 关键标记和禁止标记核验通过。
- 最终便携版：`dist\MissAV_Manager_v0.1.30.exe`；最后写入 `2026-07-23 23:34:42`；大小 `418,735,277` 字节；文件版本/产品版本 `0.1.30`；SHA-256 `16364D7FCFEE169A21D3065262ADC46F52FC1AAA3D3828E942DA7B149CEA4C64`。

## 53. 处理页番号文件多选导入（2026-07-23）

- 版本升为 `0.1.31`。处理页不再区分“导入文件”和只有图标提示的“批量导入”；统一入口始终向系统文件对话框请求多选。
- 一次可选择 TXT、HTML、HTM、MD、JSON、CSV 和 LOG。成功读取的内容按文件对话框返回顺序用空行分隔并追加到当前原文，再由现有过滤器统一规范化和本次输入去重。
- 单个文件读取失败不再导致整批导入丢失。其余成功文件正常进入输入框和“一行一个”的过滤结果；提示、持久日志分别记录请求数、成功数、失败数和失败文件名。
- 101 项单元测试、静态检查和完整隔离 Electron UI 回归通过。专项测试确认文件对话框请求 `multiSelections=true`，一个 HTML 与一个 TXT 合并后识别 3 个唯一番号，跨文件重复只保留首次出现项。ASAR 同时确认新入口存在、旧双按钮入口不存在。
- 最终便携版：`dist\MissAV_Manager_v0.1.31.exe`；最后写入 `2026-07-23 23:45:59`；大小 `418,735,548` 字节；文件版本/产品版本 `0.1.31`；SHA-256 `35ADD5EFACD2C8ADD36D9F9F0835E8C0B442826BEF52C0F8D54BAD547B20962E`。

## 54. 正式启用前全库归零与高级数据编辑（2026-07-24）

- 版本升为 `0.1.32`。备份恢复页新增“正式启用前归零 / 全库重新开始”：用户必须输入 `清空全部数据`，软件先创建可恢复的完整 SQLite 快照，再在事务中枚举并清空全部用户业务表。
- 归零范围包括永久番号、女优/类型 Tag 与关系、处理批次/逐条结果/四阶段任务、站点查询缓存、Raindrop 远端映射、Telegram 来源/消息指纹/导入历史、旧收藏兼容表，以及数据库中仍存在的未知旧版遗留表。自增序号会重置，随后执行 `VACUUM` 和 `PRAGMA integrity_check`。
- 归零不删除数据库结构、任何备份文件、外观设置，也不删除 Windows `safeStorage` 中的 Chrome 扩展配对密钥、Raindrop Token、Telegram Bot Token、API 凭据或加密会话。查询、收藏或同步运行时界面拒绝执行。
- 高级数据工作台补齐 `site_lookup_cache` 和 `remote_sync_records`，并为批次结果 JSON、任务元数据、Telegram 来源/指纹/历史开放经过校验的安全编辑字段。
- 表格按核心数据、关系数据、任务历史、缓存与同步、Telegram 分类；新增单击、Ctrl、Shift、Ctrl+Shift 多选、选择本页、批量改值、批量删除、复制 TSV 和导出 CSV。批量写入只落盘一次，操作前自动备份；番号唯一性、外键主键和 JSON 格式继续校验。
- 修复备份恢复页面旧按钮只有界面没有处理逻辑的问题：创建、刷新、打开目录、复制路径、恢复和删除现在均有实际行为，恢复前继续自动备份当前库。
- 新增两项隔离数据库测试，覆盖全表备份清空、旧/未知表枚举、ID 重置、恢复往返、JSON 校验、批量修改/删除和表格导出；自动测试增至 103 项。正式数据库只读盘点在归零前为 58,764 行且完整性为 `ok`，其中发现并纳入 18,659 行旧 123AV 账号执行表。
- 正式数据库先生成 `missav_data_20260724_001050_正式启用前完整备份.db`，再将 17 张现存用户表从合计 58,764 行归零为 0 行；压缩后数据库为 253,952 字节，`PRAGMA integrity_check` 为 `ok`。隔离打包版首启也成功创建 15 张当前版本业务表、0 行空库和 `0.1.32` 日志。
- 最终便携版：`dist\MissAV_Manager_v0.1.32.exe`；最后写入 `2026-07-24 00:12:06`；大小 `418,766,109` 字节；文件版本/产品版本 `0.1.32`；SHA-256 `D65F3E911462C165AE38D60BECA00917D4C87EA7D28E757822212D8ABD3BC566`。

## 55. TG 内容工具箱与 MissAV 自动分流（2026-07-24）

- 版本升为 `0.2.0`，界面标题改为“TG 内容工具箱”。侧边栏以推特博主、Bad.news、MissAV、123AV 四个工具入口为第一层；MissAV/123AV 再按输入、执行、结果、Raindrop 分级，公共 Telegram、数据库、日志/备份/外观归到独立设置区。
- 推特工具识别 `#handle`、`@handle`、x.com/twitter.com 主页，按首次出现顺序与大小写不敏感规则去重，分别输出一行一个的博主名和主页链接。Bad.news 工具只保留 `https://bad.news/t/数字`，去除 `/app`、其他官网路径、查询参数和片段。
- 两个文本工具都支持粘贴、多文件 HTML/JSON/TXT/MD/CSV/LOG、精确到分钟的起止范围、复制和 TXT 导出；结果只存在当前会话，不建立永久业务结果库。
- Telegram 保持一个 Bot 全局更新队列和最多 5 个群的安全边界。四个工具分别保存群 `sourceKey` 绑定；同一群可同时绑定多个工具，接收到的消息会独立投递，不会互相抢占。
- MissAV 与 123AV 的输入、准备批次和有效任务支线隔离。新批次写入 `tool_kind`；MissAV 只启用查询/Raindrop，123AV 只启用查询/收藏；旧 `dual` 批次兼容。
- MissAV 批次创建时冻结已有女优 Tag 到 `known_actresses_json`。Raindrop 预览逐条判断：任一女优已在快照中进入根目录 `missav1`，否则进入 `missav2`；同批中新女优不会污染判断。目录缺失时通过官方 API 自动创建，同步继续保持预览后手动确认。
- 新增推特/Bad.news/分钟范围、站点任务隔离、女优快照和 Raindrop 分流测试；109 项单元测试、静态检查和隔离 Electron 全界面冒烟通过，正式 SQLite 未参与测试写入。
- 最终便携版：`dist\MissAV_Manager_v0.2.0.exe`；最后写入 `2026-07-24 11:41:02`；大小 `418,817,176` 字节；文件版本/产品版本 `0.2.0`；SHA-256 `0827BFD3D8685CEDC5D945411D95603CB72FF3EA395682B3B635FA45624F2675`。ASAR 核验通过，唯一临时用户目录隔离首启成功创建空数据库与 `0.2.0` 运行日志，测试进程和临时目录均已清理。

## 56. 工具注册架构、原生 SQLite 与自选数据库目录（2026-07-24）

- 版本升为 `0.3.0`，产品和 EXE 正式改名为“TG 内容工具箱 / `TG_Content_Toolbox`”。保留原 `appId` 和旧用户数据目录自动发现，升级不会因为外壳改名而丢失现有数据库、令牌或会话。
- 侧栏收敛为工具首页、任务中心、Telegram 来源、数据中心、日志/备份/外观 5 个全局入口。四个工具由 `src/tools/registry.js` 注册并在专用首页按“文本提取 / 影片工具”分类；进入 MissAV/123AV 后才显示该工具的输入、执行、结果和同步二级阶段。
- 推特、Bad.news 的规则与 Telegram 时间处理拆入独立模块；首页外壳拆为 `renderer/tool-shell.js`。新增任务中心统一显示两站查询、123AV 收藏、Raindrop 和 Telegram 状态，后续增加文本工具不再继续堆侧栏。
- 数据库从渲染器 sql.js 改为主进程 `node:sqlite`，启用 WAL、外键、忙等待、FULL 同步和显式检查点。旧标准 SQLite 文件首次打开前自动建立一次 `v0.3.0_native_migration` 备份并写迁移标记。
- 番号管理改为数据库端搜索、状态筛选、排序、ID 选择和分页，每页上限 500，不再一次把 5 万条塞进页面。历史导入使用单事务和复用语句；100,200 条隔离压测约 1.2 秒导入，首屏约 0.12 秒，精确搜索约 0.09 秒。
- 设置页新增数据库目录“打开 / 更改位置”。用户可选择项目内的专用空目录；软件先创建备份并做 FULL 检查点，复制数据库、迁移标记和备份目录，校验新库后保存位置并重启。原数据库不删除，失败会恢复使用原目录。
- 主窗口启用 Chromium sandbox 和 CSP；preload 仅加载 Electron IPC，不再加载 fs、path、数据库或业务 Node 模块。主进程对数据库方法、业务方法、网络主机和用户选择路径分别使用白名单。
- 修复 electron-builder 旧 `src/**` / `assets/**` 规则不会递归带入新子目录的问题，改为递归 glob。包核验确认 4 个工具模块和 5 个嵌套视觉资源均存在，运行包不再包含仅供测试使用的 sql.js，因此便携包由约 418.8 MB 降至约 399.8 MB。
- 静态检查、117 项单元测试、全桌面/416px UI 冒烟、Telegram Bot/API/导出、双站查询、收藏串行、Raindrop、工具首页和任务中心均通过；所有测试使用临时 SQLite 与模拟账号。最终便携 EXE 在唯一临时目录无界面首启成功，退出码 0，创建 241,664 字节原生 SQLite 空库，日志包含 `0.3.0`、`node:sqlite` 和窗口就绪标记；测试目录已删除。
- 最终便携版：`dist\TG_Content_Toolbox_v0.3.0.exe`；最后写入 `2026-07-24 12:52:11`；大小 `399,780,545` 字节；文件版本/产品版本 `0.3.0`；SHA-256 `94841863BEA9AFB9AADFB6992F92E41EAF014283DB4DAFE169ED6F4EF9530702`。
