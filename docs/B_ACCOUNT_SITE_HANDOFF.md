# B 账号 ChatGPT Site 发布交接表

更新日期：2026-08-01

> 2026-08-01 更正：第 5、7、17 项中任何“Telegram 个人 API 仅留在 Windows”的旧选择均已撤销。正确范围是 Windows 保留，同时网站实现 Bot API 与个人账号 MTProto；两端 Session 和检查点彼此独立。Telegram 网站功能在真实登录、来源发现、历史、增量、已读和安全测试完成前保持“未完成”。

## 填写说明

- 把选项前的 `[ ]` 改为 `[x]`；不确定就保留“按推荐”。
- **不要在本文件填写 B 账号密码、Telegram Token、API Hash、验证码、Cookie 或其他密钥。**
- 填完保存文件，然后在 Codex 中回复“已填好”。
- 当前仓库尚无 `.openai/hosting.json`，没有绑定 A 或 B 的 Site 项目。

## 已确定的安全发布路线

```text
A 账号 Codex：开发、测试、提交源码
                 ↓
             Git 仓库
                 ↓
B 账号 ChatGPT：创建 Site、配置 Secrets、保存版本、正式部署
```

- A 账号不登录 B 账号，也不接触 B 的密码。
- B 账号第一次创建 Site 后产生的项目绑定只用于 B 的正式站点。
- 网页版放在独立目录 `apps/web-site/`，不覆盖当前 `apps/desktop-v05/`。
- 建议新建开发分支 `codex/sites-private-web`；当前桌面主线 `codex/v0.5-redesign` 保持可回退。

---

## A. B 账号准备情况（必须回答）

### 1. A、B 是什么关系？

- [x] 两个完全独立的 Plus/Pro 个人账号
- [ ] 同一个 Business/Enterprise 工作区的两个成员
- [ ] 目前不确定

回答：

### 2. B 账号现在是否能看到 ChatGPT Sites？

- [x] 能，在 ChatGPT 的 Work/Sites 中已经能创建网站
- [ ] B 有 Plus/Pro，但暂时看不到 Sites
- [ ] 还没检查

回答：

### 3. 谁来完成 B 账号内的操作？

- [x] 我本人切换到 B 账号，按 Codex 给出的步骤点击创建和部署（推荐）
- [ ] B 账号的实际使用者操作
- [ ] A、B 都是我本人，可以在同一台电脑切换登录

回答：

### 4. B 账号能否访问当前 GitHub 仓库？

- [x] 能，仓库公开或 B 已有权限
- [ ] 不能，需要稍后邀请 B 的 GitHub 账号
- [ ] 不走 GitHub，届时使用本地项目副本

回答：

> 如果需要邀请 GitHub 账号，只填写 GitHub 用户名；不要填写密码或访问令牌。

GitHub 用户名（可选）：

---

## B. 第一版网站范围（决定工作量）

### 5. 采用哪条迁移路线？

- [x] 双端并行：网站实现 Telegram Bot API 与个人账号 MTProto；Windows v0.5.13 同时保留独立回退（更正后范围）
- [ ] 只实现 Bot / 导入，不实现网站个人账号 API（已撤销）
- [ ] 只先做三个文本过滤器

回答：

### 6. 第一版网站包含哪些功能？可多选。

- [ ] 推特博主过滤
- [ ] Bad.news 链接过滤
- [ ] 海角链接过滤
- [ ] MissAV 番号过滤
- [ ] MissAV 完整浏览器脚本生成
- [ ] 参考女优 Tag 库和两层黑名单
- [ ] 处理历史
- [ ] 数据中心与表格 CRUD
- [ ] 多文件导入和 TXT/CSV/JSON 导出
- [ ] Telegram Bot 增量
- [ ] 其他：

回答：复刻当前最新版的所有功能（如出现本地功能不方便设计到网站里的你可以找我手动确认）

### 7. Windows 回退与网站能力如何并存？

- [x] Telegram 个人账号 API 在 Windows 保留，同时网站完整实现；两端彼此独立
- [x] 同意：123AV 查询、Chrome 扩展和账号收藏留在本地（推荐）

回答：

### 8. Raindrop 如何处理？

- [ ] 沿用 v0.5 主线决定：网站也不恢复 Raindrop（推荐，当前代码已移除）
- [x] 网站恢复 Raindrop 导出文件，但不直接连接 API
- [ ] 网站恢复 Raindrop API 同步

回答：

---

## C. 数据与隐私（必须回答）

### 9. B 账号网站数据库从哪里开始？

- [ ] 从空库开始（推荐，风险最低）
- [x] 导入当前 v0.5 本地永久记录、Tag、黑名单和处理历史
- [ ] 只导入番号、参考女优 Tag 和两层黑名单

回答：

### 10. 网站允许保存哪些数据？逐项填写“允许/不允许”。

全部允许

- 番号与规范链接：
- 推特用户名与主页链接：
- Bad.news/海角链接：
- 参考女优 Tag 与两层黑名单：
- Telegram 消息正文：
- Telegram 群组名称、消息 ID 和时间：
- 处理历史与错误日志：
- 影片封面、图片或露骨描述：

### 11. Telegram 原文如何处理？

- [ ] 网站不保存 Telegram 原文，只接收手动上传文件并在解析后丢弃（推荐）
- [x] 处理成功后删除原文，只保留提取结果和消息 ID
- [ ] 需要永久保存原文

回答：

### 12. 当前本地数据库是否需要迁移？

- [ ] 暂时不迁移
- [x] 需要迁移；Codex 先制作脱敏迁移预览，由我确认后再导入（推荐）

回答：

> 不要现在复制正式 SQLite。确定迁移后，Codex 会提供只读导出和数量核对步骤，不直接操作正式数据库。

---

## D. Site 归属与访问（必须回答）

### 13. 正式 Site 的访问范围？

- [x] 仅 B 账号本人（推荐）
- [ ] B 账号及少数指定 ChatGPT 账号
- [ ] 同一工作区成员
- [ ] 公开互联网

回答：

### 14. 是否现在配置自定义域名？

- [x] 暂时不用，先用 Sites 自动生成的地址（推荐）
- [ ] 需要，域名是：

回答：

### 15. B 账号第一次部署时采用哪种配合方式？

- [ ] Codex 完成网站代码后，我切换到 B 账号并按步骤创建、绑定、部署（推荐）
- [ ] 先由 B 创建一个空 Site，再把生成的 `.openai/hosting.json` 放进网站分支
- [x] 目前不确定，完成本地预览后再选

回答：

### 16. 正式发布绑定如何保存？

- [ ] B 的 `.openai/hosting.json` 只放在 `codex/sites-private-web` 分支（推荐）
- [ ] 以后合并到主线，B 的 Site 成为项目唯一正式站点

回答：先不回答，你可以自己做决定

---

## E. Secrets 与账号操作

### 17. 如果第一版需要服务器端密钥，是否接受以下规则？

- [x] 接受：密钥只由我在 B 账号 Site 设置的 Secrets 中填写，不发给 Codex、不提交 Git（推荐）
- [ ] 不接受使用任何服务器端密钥

回答：

可能需要的密钥名称只列名称、不填写值：

- `TELEGRAM_BOT_TOKEN`
- `TELEGRAM_API_ID`
- `TELEGRAM_API_HASH`
- `TELEGRAM_SESSION_ENCRYPTION_KEY`
- `MTPROTO_BACKEND_URL`（仅在实测需要私有后端时）
- `MTPROTO_BACKEND_TOKEN`（仅在实测需要私有后端时）
- 其他：

`TELEGRAM_SESSION_ENCRYPTION_KEY` 是网站个人账号 Session 的独立 AES-GCM 加密密钥，必须作为 Secret 保存，值至少 32 个随机字符；它不能与 Bot Token、API Hash 或 Windows Session 共用。没有该项时网站必须禁用个人账号登录，不能降级为明文保存。

### 18. 是否同意以下账号安全边界？

- [x] A 不获取 B 密码、验证码和登录会话（必须）
- [x] A 不代填 B 的 Site Secrets（必须）
- [x] B 负责最终检查访问范围并点击正式 Publish（推荐）

回答：

---

## F. 验收与回退

### 19. 第一版验收标准？可多选。

- [x] 与 v0.5.13 相同输入得到相同过滤结果
- [x] 支持手机和电脑
- [x] 表格具备搜索、排序、全选、编辑、批量操作、复制和导出
- [x] 10 万条结构化数据仍可分页使用
- [x] 刷新网页后数据和历史不丢失
- [x] B 账号退出后其他账号不能访问
- [x] 所有 Secrets 不出现在前端、Git 和日志
- [x] 本地 EXE 可以继续独立使用和回退
- [x] 其他：

回答：

### 20. 是否允许我按下面顺序直接工作？

1. 保护当前 v0.5.13 桌面版工作区。
2. 新建 `codex/sites-private-web` 分支。
3. 在 `apps/web-site/` 建立 Sites 兼容网站。
4. 迁移已确认的纯规则和测试夹具。
5. 完成本地预览、数据测试和隐私检查。
6. 交给你在 B 账号中创建正式 Site 并生成项目绑定。
7. 写入 B 的 Site 绑定后做最终部署验收。

- [x] 允许，按上述顺序直接工作（推荐）
- [ ] 需要修改：

回答：

## 最后补充

回答：
