# TG 内容工具箱 v0.5.13（Windows）

## v0.5.13 Telegram 工具工作台与已读修复

- Telegram 来源页收敛为账号/Bot 连接、发现和来源库；群组绑定、个人 API 增量同步、历史加载、筛选处理与手动标已读全部进入各工具的“输入 → Telegram 群组消息”。
- 消息工作表补齐筛选开始/结束时间、新增本地消息、时间/正文单元格编辑、当前工具范围删除、可见全选列、Ctrl/Shift/Ctrl+A、方向键、Enter 和 Delete。
- 每个来源卡片直接展示已读策略与待确认位置；修复通过个人 API 手动标已读时依赖重新拉取目标消息、可能一直失败的问题，改为按远端来源和最高消息 ID 直接标记。
- 来源发现不再要求预先选择目标工具；保存到来源库后再在具体工具中绑定，降低相似群组错绑概率。
- Bot Token 仅在当前运行内存中共享；“接收 Bot 更新”也移入工具工作页，并只写共享消息缓存与各工具独立队列，不再提前建立内容任务。

## v0.5.13 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.13.exe`
- SHA-256：`DBDEB6FF18AE037431F3FCF835D99F448C2DEA815A3FBC8297DFA7858E7B6FB1`
- 文件大小：`19,821,568` 字节（约 18.90 MiB）
- 构建日期：2026-07-29
- 验证：139 项 Node 测试、24 项 Rust 测试、TypeScript/Vite 与 Tauri Release 构建；来源页和 Bad.news 工具 Telegram 工作台浏览器冒烟通过；隔离 EXE 首启完成 `setup:database_ready` 与 `setup:ready`，创建 200,704 字节空库，正式数据库未参与测试。

## v0.5.12 Telegram 群组绑定防误操作

- 每个工具的“输入 → Telegram 群组消息”页面可直接管理当前工具的个人 API 与 Bot 群组绑定，不必在五个工具之间来回猜测。
- 绑定编辑器始终突出显示当前目标工具，并分别展示本次新增、移除数量及各群组的其他工具绑定。
- 来源发现页改为一次只指定一个目标工具；再次添加已有群组只合并新绑定，不再覆盖原有工具。
- 保存前明确确认新增和移除数量；数据库使用单事务批量更新，失败时不会留下只改一半的绑定。

## v0.5.12 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.12.exe`
- SHA-256：`DAE0BCC14BC1269AFAA0ED25D7F70F10DD221D5995016BFA7DCD975C6D30ECF4`
- 文件大小：`19,972,096` 字节（约 19.05 MiB）
- 构建日期：2026-07-28
- 验证：139 项 Node 测试、23 项 Rust 测试、TypeScript/Vite 与 Tauri Release 构建全部通过；隔离 Windows UI 已完成 MissAV 新增/移除绑定、切换 Bad.news 核对和 SQLite 最终状态验证，正式数据库未参与测试。

## v0.5.11 Telegram 输入与导出修复

- Telegram 消息工作台从工具顶部一级标签移入“输入”页，与“手动 / 文件”成为两种并列输入方式。
- 群组/频道卡片改为左对齐双列布局，名称可换行完整显示，类型与同步位置分层展示。
- Telegram 消息表新增“复制所选消息”，直接复制所选原文。
- 补齐 Tauri `dialog:allow-save` 权限，修复 TXT、CSV 和 JS 保存时的 `plugin:dialog|save not allowed by ACL`。

## v0.5.11 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.11.exe`
- SHA-256：`4331A3CD7060081D8BD8F21A4D5AAABF812A2E132C7035C83A54BA5F789A4DFB`
- 文件大小：`19,961,344` 字节（约 19.04 MiB）
- 构建日期：2026-07-28
- 验证：139 项 Node 测试、22 项 Rust 测试、TypeScript/Vite 构建、Tauri Release 构建、隔离 Windows 界面检查，以及所选消息复制和 TXT/CSV 真实保存全部通过；正式数据库未参与测试。

## v0.5.10 每个工具独立的 Telegram 消息工作页

- 五个工具都新增“Telegram 消息”标签，只显示绑定给当前工具的个人 API 群组/频道，并支持同时选择多个来源。
- 支持最近消息、指定时间范围、继续向前和增量向后四种加载方式；每来源可扫描 1–100,000 条，网络层自动按 5,000 条分页并可安全停止。
- 新增共享 Telegram 正文缓存和五套独立工具队列。同一条消息只存一份正文，推特、Bad.news、海角、MissAV、123AV 的待处理/已完成/无结果/忽略状态互不覆盖。
- 消息工作表支持搜索、状态/候选筛选、Ctrl/Shift/Ctrl+A、当前页全选、跨页全选、处理所选、忽略/恢复、TXT/CSV 导出和全屏表格。
- 工具工作页的历史加载和手工稀疏选择不会改变 Telegram 已读位置，也不会推进来源页日常增量游标。
- 来源页文案与职责重新整理为账号登录、群组绑定、日常自动同步和已读策略。

## v0.5.10 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.10.exe`
- SHA-256：`2AEA3373DFA7024F0B73FE5136829C67C72A3DD1FA8E6DD7528EDA132E3A296E`
- 文件大小：`19,957,760` 字节（约 19.03 MiB）
- 构建日期：2026-07-28
- 验证：139 项 Node 测试、22 项 Rust 测试、TypeScript/Vite 构建、Tauri Release 构建、隔离 Windows 工具页/来源页/滚动/表格展开检查与隔离 EXE 首启全部通过。首启空库 `integrity_check=ok`，Schema 506，未使用正式数据库。

## v0.5.9 Telegram 已读策略与安全增量

- 个人 API 来源新增三种独立已读策略：增量安全入库后自动标记、从不自动标记、手动确认后标记。
- 历史回拉始终不会自动修改 Telegram 已读位置；“从现在开始”也不会把绑定前的旧消息误标已读。
- 本地分开保存安全可标位置、实际已标位置和历史基线；来源表新增“已读策略”与“Telegram 已读状态”两列。
- 手动模式只有选中一个真正待确认的来源时，“确认并标已读”才可用。标记失败会保留本地落库结果和可重试位置。
- 修复大量积压消息超过单次上限时检查点跳到最新消息、导致中间消息被跳过的问题。增量分页现在从旧到新连续推进。

## v0.5.9 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.9.exe`
- SHA-256：`A5C74A9354E5D18ACEE714270035FD0C28306B1443E4BFD23700452CC8CB8504`
- 文件大小：`19,727,872` 字节（约 18.81 MiB）
- 构建日期：2026-07-28
- 验证：139 项 Node 测试、20 项 Rust 测试、TypeScript/Vite 构建、Tauri Release 构建、隔离 Windows 来源页界面检查与隔离 EXE 首启全部通过。首启空库 `integrity_check=ok`，Schema 505，未使用正式数据库。

## v0.5.8 两层黑名单改为项目 TXT 数据源

- 项目根目录新增 `missav-blacklists`，固定保存 `1-参考女优Tag库黑名单.txt` 与 `2-Raindrop导出黑名单.txt`；文件不存在时由 APP 自动创建。
- 两个 TXT 是黑名单的唯一数据源。用户既可在 APP 内编辑并保存，也可直接用文本编辑器修改；设置页可手动重新读取，每次生成 MissAV 浏览器脚本前也会自动重读。
- 第一层黑名单改为可逆的运行时筛选，不再物理删除完整参考女优库中的 Tag；从 TXT 删除人物后会恢复参考命中资格。
- 旧数据库中的两层黑名单仅在首次启用且对应 TXT 为空时自动迁移一次。初始化完成后，空 TXT 会被正确保留，不会被旧数据库内容重新填回。
- 开发目录运行时使用项目根目录；将 EXE 单独复制给第三方时，会在 EXE 同目录自动建立 `missav-blacklists`，便于便携使用。

## 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.8.exe`
- SHA-256：`49F7ED0FEED82C8B0FEE944EBF4FD3127149411B6F8CC875284A94AA99941F1F`
- 文件大小：`19,717,120` 字节（约 18.80 MiB）
- 构建日期：2026-07-28
- 验证：139 项 Node 测试、19 项 Rust 测试、TypeScript/Vite 构建、Tauri Release 构建、外部 TXT 重读和 APP 回写 TXT 的隔离 Windows UI 冒烟全部通过。

## v0.5.7 MissAV 两层独立黑名单

- 设置页将人物过滤明确拆成两层，分别持久化，不能相互替代或合并。
- 第一层“参考库黑名单”只把对应 Tag 从参考女优库中移除。影片本身仍可进入 Raindrop；如果没有命中其他参考女优，通常会落入 `其他`。
- 第二层“Raindrop 导出黑名单”是整条影片的硬排除规则。命中任一 Tag 后，该影片不会写入 Raindrop 书签 HTML 或导入 CSV，也不会进入三个导入文件夹。
- 被第二层排除的记录仍完整保留在处理报告 CSV 与 JSON 备份中，并记录命中的人物 Tag，方便核验和追溯。
- 每次生成浏览器脚本都会固化当前参考库、参考库黑名单和 Raindrop 导出黑名单快照；之后修改设置不会改变已经生成的旧脚本。
- 脚本统计与设置页分别显示两层黑名单数量，避免误以为“从参考库移除”等于“永不导入 Raindrop”。

## v0.5.7 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.7.exe`
- SHA-256：`3EF7462E9E3085A818139F33D8EB6DEDCC13ADCA4C823A940C061B1D001B6B69`
- 文件大小：`19,700,224` 字节（约 18.79 MiB）
- 构建日期：2026-07-28
- 验证：139 项 Node 测试、17 项 Rust 测试、TypeScript/Vite 构建、脚本语法/注入与三类路由测试、Windows 设置页只读冒烟全部通过。

## v0.5.6 MissAV 参考女优 Tag 三分类脚本

- 以用户本次指定的完整浏览器脚本为唯一模板基线；APP 只替换 `CODE_TEXT`，并新增注入 `REFERENCE_ACTRESS_TAGS`，不会恢复 APP 内 MissAV 查询。
- 从用户提供的 `Miss_AV.html` 复核 3,680 条书签、1,848 个源 Tag，结合旧版女优/类型字典，得到 1,553 个参考女优 Tag；`需要查找`、`#未知女优` 和类型 Tag 不会混入参考库。
- 设置页新增参考女优 Tag 库：支持逐行增删修改，也支持导入新的 Raindrop/浏览器书签 HTML，经预览数量与确认后完整替换当前参考库。
- 每次生成脚本都会固化当前参考库快照，界面显示实际注入数量。旧脚本不会被之后的参考库修改影响。
- 脚本导出的书签 HTML 始终创建 `参考女优Tag命中`、`需要查找`、`其他` 三个文件夹；“需要查找”优先，三个目录为空时也保留。
- CSV、处理报告、备份 JSON 和 Console 统计均改为三分类字段与数量，移除旧 `MissAV_Import` / `manual_verify` 路由命名。

## 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.6.exe`
- SHA-256：`0BE654DDC8E80BE5AE480D6DA22E5C68BB5E3EF5C5186CC49AF38B6A6285CEDC`
- 文件大小：`19,698,688` 字节（约 18.79 MiB）
- 构建日期：2026-07-28
- 验证：139 项 Node 测试、17 项 Rust 测试、TypeScript/Vite 构建、真实 HTML 提取、脚本语法/注入和隔离 Windows UI 冒烟全部通过。

## v0.5.5 产品重构

- 全 APP 的业务数据表统一换成表格工作区：单击、Ctrl/Shift/Ctrl+Shift、Ctrl+A、当前页/筛选结果全选、逐格编辑、大字段编辑、查找替换、撤销/重做、批量修改/删除、TSV 复制和 CSV 导入/导出。列宽、顺序、筛选与隐藏状态会持久化。
- 数据中心显示完整字段和右侧详情，使用 100k+ 服务端分页与跨页选择；工具结果、历史、Telegram 来源、123AV 账号任务和日志也共用同一表格交互。
- MissAV 已删除 APP 内查询、速度、ETA 和异常重跑，改为完整浏览器脚本生成器。可按当前任务全部或所选番号生成，一键复制、保存 `.js`、导入新模板或恢复默认模板。数据库只保存生成元数据，不重复存放整段脚本。
- 按产品决定完全移除 Raindrop 界面、请求和 Token 逻辑；旧库兼容字段仅作只读归档。
- Telegram 支持同步全部来源或仅同步表格中所选来源。多工具任务、消息指纹、检查点和任务收件箱原子落库；任一步失败都不推进检查点。个人 API 完整落库后可标记至最高处理消息为已读。
- 新增统一处理中心，按“新收到 / 已过滤 / 待网站操作 / 待复查 / 异常 / 已完成”管理任务并直接打开原处理记录。
- v0.4.5 迁移会先备份 v0.5，再在一次用户确认的替换中归档 19 张旧表，迁移永久记录、历史、Telegram 来源、真实工具绑定与旧消息去重指纹。

## 发布文件

- 文件：`dist/TG_Content_Toolbox_v0.5.5.exe`
- SHA-256：`23B7D7BF37154BDD410992868BFFDE9DCA2682B4A6BAE34C37E550A9F3969382`
- 文件大小：`19,682,816` 字节（约 18.77 MiB）
- 构建日期：2026-07-27

## v0.5.4 修复

- 修复二维码和手机号登录长期停在“正在初始化 Telegram 本机会话”的问题。现场日志确认任务尚未建立任何 Telegram 网络连接，因此与 API 信息、Clash 节点和 Telegram 风控无关。
- 移除 Telegram 会话路径上的 `libsql`：它在部分 Windows 环境会阻塞异步执行线程，连普通超时器也无法触发。
- 改用小型本地会话文件，并使用 Windows DPAPI 按当前 Windows 用户加密；认证密钥不会以明文 JSON、SQLite 或日志形式保存。
- 会话读写为同步、有限的本地操作；新增真实落盘、加密和重开测试，完整往返无需网络且不会卡住登录按钮。
- 继续保留后台登录、阶段提示、二维码自动确认和即时取消；首次登录仍应使用二维码或手机号。

## v0.5.3 修复

- Telegram 二维码、手机号和恢复会话改为与 v0.4.5 相同的后台授权模式：启动命令立即返回，不再用一次长请求锁死全部按钮。
- 连接阶段持续显示“初始化本机会话、连接 Clash、检查账号、请求二维码/验证码”等真实进度。
- 本地 Telegram 会话初始化限时 8 秒；读取代理设置或启动授权超过前端保护时间会自动解锁。
- 连接、授权、二维码和验证码阶段均提供“立即取消并解锁”；取消操作不等待被卡住的网络锁。
- 首次登录没有已保存会话时，“恢复已保存登录”保持禁用并明确提示使用二维码或手机号。
- 登录状态查询与 Telegram 网络/session 内层锁完全分离，即使后台连接卡住，界面仍能刷新状态和取消。

## v0.5.2 修复

- Telegram 个人账号登录恢复 v0.4.5 已验证的连续授权逻辑：二维码自动轮询、到期自动刷新、扫码后自动完成，手机号登录继续支持验证码与两步验证。
- Clash 的 HTTP/Mixed Port 会继续按 v0.4.5 规则转换为 Telegram 所需的 SOCKS5；连接与发送验证码增加明确超时，不再让按钮长时间无反馈。
- MissAV 查询恢复 v0.4.5 的共享请求门、自适应退速、429 断路器、403/超时冷却和稳定后渐进提速。
- 最近 10 次请求中至少 8 次返回 403 时，判定为 Cloudflare 持续浏览器验证并自动停止整批，避免把几千条尚未查询的番号继续写成网络错误。
- 网络异常支持不选记录时“一键重跑全部异常”，选中记录时只重跑选中异常。
- Telegram 错误继续遮蔽手机号、API 凭据和二维码 Token。

## v0.5.1 修复

- Telegram Bot 网络错误不再显示或写入包含 Token 的完整请求网址。
- 读取旧日志时也会即时遮蔽 Telegram Bot Token；已经暴露过的 Token 仍应在 BotFather 中撤销。
- 超长错误信息会在提示框内自动换行，不再横向撑破页面。
- Telegram 连接失败改为简短中文分类提示，便于检查 Clash、代理、DNS 和防火墙。

## 主要变化

- 产品重构为工具箱首页：推特博主、Bad.news、海角、MissAV、123AV 五个入口彼此独立。
- 输入统一支持粘贴和多文件导入；文本工具的结果、输出文本与处理历史独立保存。
- MissAV 负责番号过滤和浏览器脚本生成；123AV 保留独立查询、限速与账号操作。
- 新增 v0.5 永久数据库、FTS 搜索、100k+ 服务端分页、统一 CRUD、自动备份与数据库迁移位置。
- Raindrop 功能已从当前产品完全移除。
- Telegram 同时支持个人账号 API 和 Bot API：个人 API 支持扫码、手机号、2FA、Clash SOCKS5、群/超级群/频道、多选绑定及检查点增量；Bot 保留轻量增量入口。
- 123AV 保留 Chrome 扩展、APP 内串行助手、CSV 导出三种账号操作。
- 默认护眼绿色、无背景图；数据库、日志、来源和备份均有独立入口。

## 运行要求

- Windows 10/11
- WebView2 Runtime（Windows 11 通常自带）
- 访问 Telegram 或 123AV 时，需要你的网络/代理本身可用。MissAV 网络操作在你手动粘贴的浏览器脚本中运行。

这是未签名的本地工具。SmartScreen 出现提示时，请先核对来源与 SHA-256。应用不会导出或记录 Cookie、密码、浏览器 Storage、Telegram 验证码或二维码 Token。

若本机 Windows 策略禁止监听 `127.0.0.1` 本地端口，应用会照常启动；仅“Chrome 扩展”账号操作会显示不可用提示，APP 内串行助手和 CSV 导出不受影响。

完整教程见 [README](README.md)。
