# TG 内容工具箱 v0.4.5（Windows 便携版）

这是面向普通 Windows 用户的单文件便携版。下载本页附件中的：

```text
TG_Content_Toolbox_v0.4.5.exe
```

双击即可运行，不需要安装 Node.js、Git 或其他开发环境，也不需要执行 BAT、VBS 或命令行脚本。

文件大小：`400,093,453` 字节

SHA-256：`F147199C0DC1261D9B9DAA878A93E48444F8B547C22C50561A6D4C0B4CE8D340`

## v0.4.5 Telegram HTML 番号过滤完整修复

- 支持 Telegram Desktop 导出中的负数消息 ID，避免把整份 HTML 当作一条消息。
- 不再跨消息换行拼接番号；过滤 Whos.tv、MissAV Daily、无码后缀时间、用户名年龄、“几分钟前”和文件大小等元数据噪声。
- 数据中心“导入去重”也改为按 Telegram 消息逐条读取 HTML/JSON，不再直接扫描整份网页源码。
- 用户 7 份真实 HTML 回放共 6,191 条消息；65 个已确认假番号全部消失，35 个抽查真番号全部保留。
- 139 项自动测试全部通过，正式数据库未参与测试。

## v0.4.4 Raindrop 全账号番号去重

- 生成同步预览前分页读取 Raindrop 全账号收藏；官方 `collectionId=0` 范围包含所有 Collection，但不包含回收站。
- 使用规范化番号比较本地永久库和 Raindrop 全账号，不再把 Collection 位置当作重复判断条件。
- 同一番号两边都存在时显示 `双方已有·不写入`，不会更新标题、Tags、摘要、备注、封面、映射或 Collection。
- 只有官网缺少时才 Push，只有本地缺少时才 Pull；所选 Collection 继续决定 Pull 来源与 Push 目标。
- 同一番号在多个远端 Collection 重复出现时，同一轮只生成一条处理计划。
- `内容一致·不写入` 与 `双方已有·不写入` 分开显示，避免把“完整内容相同”和“仅番号两边都有”混为一谈。

## v0.4.3 Pull Tags 与 Telegram HTML 过滤修复

- Raindrop Pull 的全部官网 Tags 会写入本地 `Raindrop / 最终 Tags`，并在永久番号库列表与详情中直接显示和编辑。
- 官网 Tag 与本地已有女优或类型词库同名时，自动补上对应本地关系；无法判断类型的 Tag 仍作为最终 Tag 保留，不会丢失或乱分。
- Telegram HTML 新增对 `missav.ws` 详情链接的可信识别，保留原始详情链接作为番号来源。
- 过滤前屏蔽 `800×540` 图片尺寸和 `161.2 KB`、`1.4 MB` 等媒体信息，不再把 KB 与后续数字拼成假番号。
- 使用用户提供的三份 Telegram HTML 回放：2,490 条消息提取 2,331 个唯一番号，`KB-12/15/16/20/23` 均未出现。
- 原“无需变化”改为“内容一致·不写入”，并在界面解释它表示本地与官网同步字段完全一致，本轮不会重复 Pull 或 Push。

## v0.4.2 Raindrop 页面滚动修复

- 修复桌面窗口下 Raindrop 同步中心超过可视高度后被裁切、无法继续滚动的问题。
- Raindrop 整页现在使用独立纵向滚动区，可以稳定到达页面最底部。
- 锁定应用根页面的横向移动，避免滚轮、触控板或内部宽表导致顶栏和侧栏整体偏出窗口。
- Collection 选择器与同步预览表仍保留各自的内部滚动，不会与整页滚动互相抢占。
- 已在桌面和 416px 窄屏环境验证；桌面测试页可从 `0` 滚动到 `1062.4px` 的实际底部，文档横向溢出为 `0`。

## v0.4.1 Raindrop 可选方向同步

- 同步中心新增三种可选模式：Pull（官网到本地）、Push（本地到官网）和双向同步。
- Collection 改为可搜索的多选范围，最多选择 100 个，可选择同时扫描子 Collection；未选择的目录不扫描、不导入、不修改。
- Pull 只接收可信 MissAV 详情链接；远端新番号会加入永久库，已有番号只更新同步字段和胜出的 MissAV 链接，不破坏女优/类型关系。
- 双向同步保存远端 ID、Collection 与内容哈希快照，能区分本地单边修改、官网单边修改、双方同时修改和完全无变化。
- 双方同时修改时必须逐条选择保留官网、保留本地或本轮跳过；没有选择前不能执行。
- 无变化记录不再重复写库；官网删除不会自动删除本地数据，书签移到未选择目录后退出当前范围。
- 同一远端 ID 的链接番号与本地映射不一致时拒绝覆盖并提示核对，防止串号。
- 官网 CSV 导出继续保留，固定按 Push 规则生成，不会触发 Pull。
- 已通过 133 项单元测试，以及桌面、416px 移动端 Raindrop 三模式隔离 UI 冒烟。

## v0.4.0 主数据与同步改造

- 永久番号库改为主数据：番号首次从文字/文件中过滤出来就以待处理状态登记；MissAV、123AV 和 Raindrop 后续分别补全链接、状态和错误。处理批次只保留来源、参数和历史，删除批次不会删除永久记录。
- HTML、CSV 或粘贴内容中的可信 MissAV 详情链接会与番号绑定并优先访问。例如 `https://missav.ai/dm15/meyd-916-uncensored-leak` 不再被丢弃后重新猜错地址。
- Raindrop 页面改为永久库全局队列，可选择全部待同步/异常/全部 APP 管理记录。默认沿用每条记录冻结的 `missav1`/`missav2` 目标，也可把整个当前范围统一放入手选 Collection。
- 自动目录不存在或旧 Collection ID 失效时会刷新并恢复；远端重复链接继续更新而不重复新建。同步失败番号单独输出，可复制或导出 TXT。
- 同时保留两条 Raindrop 路线：预览后调用官方 API 手动确认同步，以及导出官方列格式 CSV 后在网站手动导入。
- MissAV 与 123AV 结果板块各自增加一行一个的番号框和链接框；未找到、网络错误也输出对应实际/候选链接。
- Telegram 个人账号 API 新增“完整同步成功后标记已读”，默认关闭；只在当前来源完整写入本地后执行。Bot 不具备该功能。
- 数据库 Schema 升为 302；单元测试 127 项、完整桌面/416px UI、Telegram 桌面/移动端冒烟和包内静态检查均纳入新版验收。

## v0.3.7 修复

- 修复 Raindrop 远端核对把官方 `HTTP 200 + {"result":false,"ids":[]}` 误报为失败的问题；无重复链接现在会正确进入“新建”预览。
- 结果页表格默认扩大可视高度，滚动条加宽，支持右下角拖动调整高度和“展开表格”工作区模式，`Esc` 可还原。
- 数据中心增加独立“高级表格”一级标签；结果、批次、处理历史、Telegram 来源和 Raindrop 同步页可直达对应数据表。
- 公开业务表补齐新增、搜索、逐格编辑、批量修改、单条/批量删除和导出；新增与写入前自动备份，并校验父批次、父来源、唯一键、数字和 JSON。

## v0.3.6 修复

- 刷新个人账号 Telegram 群组/频道列表时，自动识别数据库中已不再可访问的旧绑定。
- 旧来源会显示“账号已退出或暂时不可访问”和“移除绑定”按钮。
- 移除只停用增量来源，不删除 Telegram 消息历史、导入记录或番号数据。
- Bot/API 两种来源卡片均支持手动移除绑定。

本版修复 Telegram 账号退出群组后的旧来源清理问题，并继续保留 v0.3.5 的多来源选择器和五工具处理历史。

## 第一次使用

1. 下载 EXE 到任意可写文件夹。
2. 双击启动。便携版约 400 MB，第一次启动需要解压并接受 Windows 安全扫描，部分电脑可能等待约一分钟。
3. 本软件目前没有数字签名；如果 Windows SmartScreen 显示“未知发布者”，请确认文件来自本仓库 Release，再选择“更多信息 -> 仍要运行”。
4. 软件会为每位用户创建独立的本地数据库，不会附带发布者的番号、Telegram 消息、Token、Cookie 或账号数据。
5. 打开“工具首页”，选择需要使用的工具。

## 五个独立工具

### 推特博主

从 Telegram 消息、粘贴文字或多个导出文件中提取博主名，并分别输出：

- 一行一个博主名；
- 一行一个 `https://x.com/用户名` 主页链接。

### Bad.news 帖子

提取规范的 `https://bad.news/t/数字` 帖子链接，自动去除 `/app`、下载页、查询参数和官网其他无关链接。

### 海角帖子

- 使用 18 份真实 Telegram HTML、17,739 条消息校准，样本回放得到 723 条唯一正文链接；
- 只接收 `hjjd`、`hjmz`、`hjyc`、`hjfn`、`hjsz`、`hjrq`、`hjhj` 七类正文路径；
- 统一输出 `https://www.haijiaolove.xyz/栏目/数字.html`；
- 排除 `t.me` 广告/机器人、旧站 `haijiao.com/post`、栏目首页及其他无关链接；
- 支持多个群组/频道绑定、分钟范围、多文件导入、复制、TXT 导出和手动保存处理历史。

### MissAV

- 从 Telegram、TXT、HTML、JSON、CSV 等内容中过滤和规范番号；
- 查询 MissAV 详情页并抓取女优 Tag、类型 Tag；
- 维护永久番号库、批次、标签、异常和导出记录；
- 支持网络错误重跑、按标签导出和预计剩余时间；
- Raindrop 同步前必须人工预览和确认；
- 根据批次创建前的女优底库，把结果自动预分流到根目录 `missav1` 或 `missav2`。

### 123AV

- 与 MissAV 使用完全独立的输入、批次、速度和结果页面；
- 查询必须由用户手动开始，不会因为运行 MissAV 而自动启动；
- 查询命中后可选择 Chrome 扩展、APP 内执行器或仅导出；
- 同一网站的账号收藏固定单路执行，避免并发触发频率限制；
- 已收藏项目会跳过，状态不明确时进入复查，不会盲目重复点击。

## Telegram 来源

最多选择 100 个指定群组或频道，可使用：

- Telegram Bot：推荐用于以后新增消息；
- Telegram Desktop HTML/JSON 导出：推荐用于过去历史；
- Telegram 个人账号 API：可选高级入口。

每个工具可同时绑定多个群组或频道，同一个来源也可以同时绑定多个工具。来源选择器支持搜索、普通单击勾选/取消、全选可见、清空和已选摘要，不需要按 Ctrl/Shift。Telegram Token、账号会话和 API 凭据使用 Windows 安全存储保护，不写入 SQLite 或运行日志。

## 处理历史

- 推特、Bad.news、海角过滤完成后，可手动保存本次规范化结果快照；不保存原始 Telegram 正文。
- MissAV 与 123AV 直接复用已有处理批次，无需重复保存。
- 每个工具的历史彼此隔离，可搜索、逐条回看、恢复/载入、导出、重命名和删除。
- 文本工具历史纳入 SQLite 备份、恢复、全库归零和高级数据编辑。

## 数据库与隐私

- 番号库使用本机 SQLite，可支持十万条以上记录；
- 支持搜索、分页、单条编辑、Windows 文件管理器式多选、批量修改和删除；
- 支持备份、恢复、数据体检、全库归零和数据库位置迁移；
- 更换新版 EXE 不会自动覆盖已有数据库；
- 正式操作前建议在“番号库 -> 维护 -> 备份恢复”创建备份；
- 软件不会随安装包上传或共享用户的数据库、Telegram 正文、密码、Token、Cookie 或浏览器存储。

## 外部账号说明

- Telegram Bot 需要用户自己的 Bot Token；
- Telegram 个人账号 API 需要用户自己的 `api_id` 和 `api_hash`；
- Raindrop 直接同步需要用户自己的 Access Token；
- 123AV 收藏需要用户在所选的 Chrome 或 APP 账号窗口中自行登录；
- 登录、验证码、CAPTCHA 和站点访问限制必须由用户手动处理。

更详细的图文和状态说明请查看仓库首页的 `README.md` 与 `使用教程.md`。
