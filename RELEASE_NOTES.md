# TG 内容工具箱 v0.5.4（Windows）

## v0.5.4 修复

- 修复二维码和手机号登录长期停在“正在初始化 Telegram 本机会话”的问题。现场日志确认任务尚未建立任何 Telegram 网络连接，因此与 API 信息、Clash 节点和 Telegram 风控无关。
- 移除 Telegram 会话路径上的 `libsql`：它在部分 Windows 环境会阻塞异步执行线程，连普通超时器也无法触发。
- 改用小型本地会话文件，并使用 Windows DPAPI 按当前 Windows 用户加密；认证密钥不会以明文 JSON、SQLite 或日志形式保存。
- 会话读写为同步、有限的本地操作；新增真实落盘、加密和重开测试，完整往返无需网络且不会卡住登录按钮。
- 继续保留后台登录、阶段提示、二维码自动确认和即时取消；首次登录仍应使用二维码或手机号。

## v0.5.3 修复

- Telegram 二维码、手机号和恢复会话改为与 v0.4.5 相同的后台授权模式：启动命令立即返回，不再用一次长请求锁死全部按钮。
- 连接阶段持续显示“初始化本机会话、连接 Clash、检查账号、请求二维码/验证码”等真实进度。
- 本地 Telegram 会话初始化限时 8 秒；读取代理设置或启动授权超过前端保护时间会自动解锁。
- 连接、授权、二维码和验证码阶段均提供“立即取消并解锁”；取消操作不等待被卡住的网络锁。
- 首次登录没有已保存会话时，“恢复已保存登录”保持禁用并明确提示使用二维码或手机号。
- 登录状态查询与 Telegram 网络/session 内层锁完全分离，即使后台连接卡住，界面仍能刷新状态和取消。

## v0.5.2 修复

- Telegram 个人账号登录恢复 v0.4.5 已验证的连续授权逻辑：二维码自动轮询、到期自动刷新、扫码后自动完成，手机号登录继续支持验证码与两步验证。
- Clash 的 HTTP/Mixed Port 会继续按 v0.4.5 规则转换为 Telegram 所需的 SOCKS5；连接与发送验证码增加明确超时，不再让按钮长时间无反馈。
- MissAV 查询恢复 v0.4.5 的共享请求门、自适应退速、429 断路器、403/超时冷却和稳定后渐进提速。
- 最近 10 次请求中至少 8 次返回 403 时，判定为 Cloudflare 持续浏览器验证并自动停止整批，避免把几千条尚未查询的番号继续写成网络错误。
- 网络异常支持不选记录时“一键重跑全部异常”，选中记录时只重跑选中异常。
- Telegram 错误继续遮蔽手机号、API 凭据和二维码 Token。

## v0.5.1 修复

- Telegram Bot 网络错误不再显示或写入包含 Token 的完整请求网址。
- 读取旧日志时也会即时遮蔽 Telegram Bot Token；已经暴露过的 Token 仍应在 BotFather 中撤销。
- 超长错误信息会在提示框内自动换行，不再横向撑破页面。
- Telegram 连接失败改为简短中文分类提示，便于检查 Clash、代理、DNS 和防火墙。

## 主要变化

- 产品重构为工具箱首页：推特博主、Bad.news、海角、MissAV、123AV 五个入口彼此独立。
- 输入统一支持粘贴和多文件导入；文本工具的结果、输出文本与处理历史独立保存。
- MissAV 与 123AV 分别查询、分别限速；同站操作串行，站点间可以并行。
- 新增 v0.5 永久数据库、FTS 搜索、100k+ 服务端分页、统一 CRUD、自动备份与数据库迁移位置。
- Raindrop 支持任选 Collection 的 Pull、Push、双向同步，全账号番号去重，Pull 也同步所有 Tags。
- Telegram 同时支持个人账号 API 和 Bot API：个人 API 支持扫码、手机号、2FA、Clash SOCKS5、群/超级群/频道、多选绑定及检查点增量；Bot 保留轻量增量入口。
- 123AV 保留 Chrome 扩展、APP 内串行助手、CSV 导出三种账号操作。
- 默认护眼绿色、无背景图；数据库、日志、来源和备份均有独立入口。

## 发布文件

- 文件：[TG_Content_Toolbox_v0.5.4.exe](dist/TG_Content_Toolbox_v0.5.4.exe)
- SHA-256：`24DECC556EBAA618FC55607BDE4919B914E080B9118519D180447C172C781268`
- 文件大小：`19,181,568` 字节（约 18.3 MiB）
- 构建日期：2026-07-27

## 运行要求

- Windows 10/11
- WebView2 Runtime（Windows 11 通常自带）
- 访问 Telegram、MissAV、123AV 或 Raindrop 时，需要你的网络/代理本身可用。

这是未签名的本地工具。SmartScreen 出现提示时，请先核对来源与 SHA-256。应用不会导出或记录 Cookie、密码、浏览器 Storage、Telegram 验证码、二维码 Token 或 Raindrop Token。

若本机 Windows 策略禁止监听 `127.0.0.1` 本地端口，应用会照常启动；仅“Chrome 扩展”账号操作会显示不可用提示，APP 内串行助手和 CSV 导出不受影响。

完整教程见 [README](README.md)。
