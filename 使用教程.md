# TG 内容工具箱 v0.5.12 使用教程

本版只面向 Windows 10/11。发布文件是 `dist\TG_Content_Toolbox_v0.5.12.exe`，不需要 Node.js、Git 或 BAT。软件是未签名的本地 EXE，SmartScreen 提示“未知发布者”时应先核对发布页的 SHA-256。

## 1. 产品结构

```text
工具首页
├─ 推特博主：提取用户名与 x.com 主页
├─ Bad.news：提取规范帖子链接
├─ 海角：提取可直接访问的正文链接
├─ MissAV：过滤番号并生成完整浏览器脚本
└─ 123AV：番号查询、账号操作与导出

管理与维护
├─ 统一处理中心
├─ 数据中心
├─ Telegram 来源
├─ 运行日志
├─ 设置与备份
└─ v0.4.5 数据迁移
```

五个工具的输入、结果和历史彼此独立。MissAV 不会自动触发 123AV，Telegram 同步也不会自动发起网站查询。Raindrop 已从 v0.5 主线移除。

## 2. 首次启动与数据库

1. 打开 EXE，进入“设置与备份”。
2. 确认数据库路径。默认库名为 `tg-content-toolbox-v05.sqlite`。
3. 需要改位置时，选择一个专用空目录。软件会先备份，再用 SQLite `VACUUM INTO` 复制，完整性检查通过后才切换。
4. 不要在软件运行时用资源管理器替换 SQLite、`-wal` 或 `-shm` 文件。

新增、批量修改、批量删除、迁移与恢复前会自动备份。永久库按 10 万行以上设计，数据中心使用服务端分页和搜索，不会一次把全库加载到界面。

## 3. 统一表格操作

数据中心、工具结果、工具历史、Telegram 来源、123AV 账号任务和日志共用同一套表格。

- 单击选中行；Ctrl 增减选中；Shift 连选；Ctrl+Shift 扩展选择。
- `Ctrl+A` 选择当前筛选结果。数据中心可跨页选择；“全选当前页”与“全选筛选结果”是两个独立按钮。
- 方向键移动，Enter 编辑，Delete 请求删除所选行，`Ctrl+C` 复制 TSV，`Ctrl+V` 粘贴表格区域。
- 单击行后，右侧显示完整字段。双击长文本单元格使用大编辑器。
- 可拖动列宽和列顺序，表头菜单可隐藏列。列宽、顺序、筛选、排序和隐藏状态自动保存。
- 支持查找替换、撤销/重做本次编辑、批量修改、CSV 导入/导出、复制行和新增记录。
- 点“展开表格”全窗口查看，`Esc` 还原。

日志表只读，这是为了保留真实诊断证据；业务数据表才提供增删改。

## 4. 手动输入与文件

从首页打开工具后，可直接粘贴内容，也可一次选择多个 TXT、HTML、HTM、MD、JSON、CSV 或 LOG 文件。如果是 Telegram Desktop 导出的多个 `messages*.html`，可全选后一次导入。

开始/结束时间可精确到分钟。所有规则先提取真实消息区块，再过滤导出页的菜单、广告、App 入口、网页脚本和无关字段。

## 5. 三个文本工具

### 推特博主

优先读取每条 Telegram 消息的 `#标签`，同时识别可信 `@handle` 和 x.com/twitter.com 主页。输出分为两个一行一个的文本区：

```text
博主名：alice
链接：https://x.com/alice
```

### Bad.news

只保留规范的 `https://bad.news/t/数字`，去除查询参数、片段、官网 App 入口、广告和无关链接。

### 海角

只保留 `https://www.haijiaolove.xyz/分类/数字.html` 形式的正文直达链接。当前校验正文分类 `hjjd`、`hjmz`、`hjyc`、`hjfn`、`hjsz`、`hjrq`、`hjhj`，过滤广告域名、栏目首页和其他垃圾信息。

## 6. MissAV：番号过滤与浏览器脚本

1. 导入文本或 Telegram 文件，点执行过滤。
2. “结果”页会显示规范番号，纯文本输出一行一个。过滤规则会排除日期、电视型号、文件尺寸、Office 版本、普通帖子 ID、标签片段与无关英数组合。
3. 首次使用可直接采用内置参考库：它从你提供的 `Miss_AV.html` 的 3,680 条书签中识别出 1,553 个女优 Tag。
4. 需要更换参考时，到“设置与备份 → 参考女优 Tag 库”：可在文本框中逐行增删 Tag，或选择另一个书签 HTML。HTML 导入会先显示识别数量，经确认后完整替换旧库。
5. 进入“浏览器脚本”。未选行时使用当前任务全部合法番号；选中行后只使用所选番号。
6. 工具会规范 FC2、去重、保持原顺序，把番号写入 `CODE_TEXT`，并把当前 1,553 个或自定义参考 Tag 写入 `REFERENCE_ACTRESS_TAGS`。界面会显示本次实际注入数量。
7. 点“复制完整脚本”。在浏览器打开 MissAV，按 `F12`，切换到 Console，粘贴并运行。
8. 脚本导出的书签 HTML 始终包含 `参考女优Tag命中`、`需要查找`、`其他` 三个新文件夹。带“需要查找”的记录优先进入该目录；否则只要命中任一参考女优 Tag 就进入命中目录，其余进入“其他”。
9. 第一层“参考库黑名单”只临时取消人物的参考命中资格：这类影片不会因该人物进入参考命中目录，但仍可能进入“其他”，或因同片其他参考女优而命中。它不会删除完整参考库；以后把该人物从黑名单 TXT 删除即可恢复。
10. 第二层“Raindrop 导出黑名单”用于永远不看的女优：只要影片命中其中任一完整 Tag，就不会写入 Raindrop HTML/CSV。处理报告和 JSON 备份仍保留该条及命中的黑名单 Tag，避免静默丢失证据。
11. 项目根目录会自动建立 `missav-blacklists`，里面有 `1-参考女优Tag库黑名单.txt` 和 `2-Raindrop导出黑名单.txt`。两个文件都使用 UTF-8、每行一个完整女优 Tag；空行和重复行会自动清理。你可在 APP 内编辑并点对应的“保存并写入……TXT”，也可关闭或保持 APP 打开时直接用文本编辑器修改。外部修改后点“重新读取 TXT”刷新界面；生成新脚本前会自动重读，因此脚本总是使用当时文件里的最新内容。
12. 也可“保存为 .js”。如果以后更新脚本，到设置页导入 JS/TXT 模板；模板损坏时点“恢复默认模板”。已经复制出的脚本保留生成当时的参考库与导出黑名单快照，不会被之后的修改悄悄改变。

APP 内已没有 MissAV 网络查询、并发、RPS、ETA 或异常重跑。访问、标签抓取和导出都由你手动运行的浏览器脚本完成。数据库只记录脚本模板哈希、生成时间、番号数和任务 ID，不保存整段脚本副本。

## 7. 123AV

123AV 继续作为独立网站工具，包含番号过滤、公开详情页查询、结果管理和账号操作。查询只使用受控详情页规律及变体，不使用慢搜索页；必须从页面可见内容核验番号。

账号操作保留三种方式：

- Chrome 扩展：复用现有 Chrome 登录，固定单路串行；频率/网络异常后休息 10 秒再继续。
- APP 内串行助手：一次只打开一条详情，由你手动确认操作结果。
- 仅导出：输出番号与已验证详情链接 CSV，不访问账号。

同一 123AV 账号操作永远只有一路。工具不读取 Cookie、密码、Local Storage、Session Storage 或完整页面 HTML。

## 8. Telegram 个人 API

### 登录

1. 在 `my.telegram.org/apps` 获取 `api_id` 和 `api_hash`。
2. 如需 Clash，先在设置页填写 `http://127.0.0.1:7890` 或自己的 Mixed/SOCKS 端口。对 Telegram 会自动换成 SOCKS5。
3. 首次优先点“生成二维码”，用手机 Telegram 的“设置 → 设备 → 连接桌面设备”扫码。二维码会轮询并到期自动刷新。
4. 也可使用国际格式手机号、验证码和两步验证密码。
5. 登录任务在后台运行，界面会显示当前阶段。如果网络长时间无响应，点“立即取消并解锁”，不需要强制关闭 APP。

API 凭据和会话由 Windows 当前用户加密，不写入业务 SQLite 或运行日志。

### 绑定来源

1. 登录后点“刷新群组/频道”。最多可保存 100 个个人 API 来源。
2. 搜索、全选可见或手动勾选多个群、超级群或广播频道。
3. 在醒目的“目标工具”下拉框中只选择一个工具，再确认添加。重复操作可把同一个群组添加给多个工具；再次保存不会覆盖它已有的其他工具绑定。
4. 首次绑定默认“从现在开始”。来源页负责日常自动分发、来源检查点和已读策略，不再承担日常人工挑选消息的主要界面。
5. 为每个来源选择已读策略：“增量安全入库后标已读（推荐）”、“从不自动标已读”、“手动确认后标已读”。
6. 需要自动处理新消息时，可点“同步已绑定个人来源”或在来源表同步所选；多工具任务、消息指纹、来源检查点与处理中心入队仍在一个 SQLite 事务中提交。

### 每个工具的 Telegram 消息工作页

1. 从工具首页打开推特、Bad.news、海角、MissAV 或 123AV，进入“输入”，再选择“Telegram 群组消息”。点击“管理当前工具绑定”，即可在当前工具内增加或移除个人 API、Bot 群组；页面会明确显示当前正在配置哪个工具、待新增/移除数量和每个群组已绑定的其他工具。
2. 选择“最近消息、指定时间范围、从本地最早处继续、从本地最新处增量”之一，并填写每个来源最多扫描多少条（1–100,000）。软件内部自动按 5,000 条分页，不会一次堵死界面。
3. 加载后在消息工作表中搜索或按状态筛选。单击、Ctrl、Shift、Ctrl+A、“全选当前页”和“全选筛选结果”均可用；可跨页保留选择。
4. 点“处理所选”后，只有这些消息进入当前工具并生成一次普通历史任务；没有提取到内容的消息记为“已处理·无结果”，方便以后区分“没处理”和“处理过但没结果”。
5. 可把消息设为“已忽略”或恢复“待处理”；“复制所选消息”会直接复制所选正文，也可导出所选原始消息为 TXT/CSV。一个来源绑定多个工具时，同一正文只在本地缓存一份，但每个工具的状态完全独立。
6. 此工作页的历史加载、时间范围和稀疏选择不会推进来源检查点，也不会自动标记 Telegram 已读。这样选中第 100 条时，不会误把前面尚未挑选的 99 条当作已处理。

## 9. Telegram Bot

1. 在 `@BotFather` 创建 Bot，复制 Token。
2. `/setprivacy` 选择该 Bot 并设为 `Disable`，让 Bot 能看到群内普通消息。
3. 把 Bot 加入目标群或频道，发一条测试消息，再在 APP 点“发现群组/频道”。
4. 多选来源，在“目标工具”中一次添加给一个工具；已有的其他工具绑定会保留。以后手动点同步，本版不做后台定时同步。

Bot 只能收到被加入后 Telegram 保留在 updates 队列中的消息，不能代替个人账号回拉完整历史，也不能替你的个人账号标已读。Token 只在当前打开的软件内存中，不写日志。

Token 泄露后，在 `@BotFather` 输入 `/mybots` → 选择 Bot → `API Token` → `Revoke current token`，然后把新 Token 重新粘贴到 APP。

## 10. 统一处理中心

手动处理与 Telegram 同步建立的任务会进入统一收件箱，按以下阶段分类：

- 新收到
- 已过滤
- 待网站操作
- 待复查
- 异常
- 已完成

可搜索、多选并批量改阶段。只选一条时点“打开所选任务”，会回到正确工具和原运行记录。

## 11. v0.4.5 数据迁移

迁移页只读打开 v0.4.5 `missav_data.db`，先做 `PRAGMA integrity_check`，再备份当前 v0.5 库。用户确认“替换当前业务数据”后才会清空 v0.5 业务表。

迁移包括：

- v0.4.5 所有非 SQLite 内部表的完整只读归档。
- MissAV 和 123AV 永久记录、女优、类型、标签与历史运行。
- Telegram 来源、检查点、从 v0.4.5 Local Storage 恢复的真实工具绑定，以及旧消息 ID 指纹，避免迁移后首次同步重复生成任务。

旧库始终以只读方式打开，不会修改。如果迁移失败，使用迁移前备份恢复当前 v0.5。

## 12. 故障反馈

1. 打开“运行日志”，用搜索、等级筛选和右侧详情找到对应时间。
2. 复制错误、时间、页面截图和操作步骤发给开发者。
3. 不要发送 Telegram `api_hash`、Bot Token、手机验证码、二维码 URL、123AV Cookie 或密码。APP 日志会主动遮蔽这些字段，但截图前仍应人工检查。

更详细的版本变更见 [RELEASE_NOTES.md](RELEASE_NOTES.md)，开发与数据库交接见 [docs/PROJECT_HANDOFF.md](docs/PROJECT_HANDOFF.md)。
