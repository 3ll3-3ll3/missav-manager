{
  "name": "tg-toolbox-sync-service",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/index.mjs",
    "deploy:dry": "wrangler deploy --dry-run"
  },
  "devDependencies": {
    "esbuild": "^0.27.0",
    "miniflare": "^4.20260515.0",
    "wrangler": "4.92.0"
  }
}
